# Dependensi notebook praktik "Rekayasa Fitur Modern"
# ---------------------------------------------------------------
# Semua notebook (Bab 1-17) memakai stack Python standar, TANPA GPU dan
# TANPA pretrained model. Ekstraksi fitur yang berat (embedding teks/citra/
# audio, dsb.) sudah diprakomputasi dan disimpan sebagai snapshot di
# data/section1/, sehingga bab modalitas lanjutan pun ringan dijalankan.

numpy>=1.26
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
matplotlib>=3.8
pyarrow>=15.0        # wajib: membaca snapshot .parquet
pillow>=10.0         # Bab 14 (menampilkan thumbnail produk)
networkx>=3.2        # Bab 13 (fitur graf)

# Catatan: di Google Colab semua paket di atas sudah tersedia secara default,
# jadi tidak ada sel "pip install" khusus di dalam notebook. Data Section 1
# terunduh otomatis dari repo saat notebook dijalankan (lihat README).
