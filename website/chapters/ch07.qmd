# Seleksi Fitur

## Relevansi, Redundansi, dan Kutukan Dimensi

Mengurangi jumlah fitur dalam *dataset* sering kali dirasa berlawanan dengan intuisi awal pemodelan. Jika data adalah sumber informasi, mengurangi dimensi dapat diasumsikan sebagai tindakan membuang pola yang berharga. Pada praktiknya, seleksi fitur (*feature selection*) dilakukan dengan tiga tujuan spesifik. Pertama, proses ini bertujuan untuk meningkatkan interpretabilitas, mengingat model yang mengandalkan belasan fitur utama jauh lebih mudah dianalisis perilakunya dibandingkan model yang menerima ratusan variabel mentah. Kedua, penyeleksian ini berguna untuk menurunkan beban komputasi; ruang fitur yang lebih kecil secara langsung mempercepat waktu pelatihan dan menghemat kebutuhan memori. Ketiga, pengurangan dimensi secara matematis membantu mencegah *overfitting* dengan mengarahkan model untuk mempelajari pola umum yang stabil, alih-alih sekadar menghafal *noise* spesifik pada data pelatihan.

Untuk melakukan seleksi fitur secara presisi, analisis perlu membedakan konsep relevansi dan redundansi secara mendasar. Fitur yang relevan merujuk pada besaran informasi yang dipegang oleh fitur tersebut untuk memprediksi variabel target; atribut dikategorikan relevan apabila perubahannya memengaruhi nilai target secara konsisten. Di sisi lain, fitur yang redundan muncul ketika variabel tersebut sekadar menduplikasi informasi yang sudah ditangkap oleh dimensi lain. Dalam struktur data, kehadiran fitur redundan sama sekali tidak menyumbang kemampuan prediktif baru.

Sebagai contoh pada pemodelan harga properti, sebuah *dataset* mungkin mencatat luas bangunan dalam kolom meter persegi dan kaki persegi. Kedua variabel tersebut sangat relevan terhadap harga. Namun, menyimpan keduanya secara bersamaan menghasilkan redundansi. Jika model sudah menggunakan dimensi meter persegi, variabel kaki persegi menjadi tidak berguna karena nilainya sebatas hasil konversi linier. Mempertahankan variabel redundan justru memperlambat komputasi dan memicu ketidakstabilan pembobotan model.

Salah satu cara matematis untuk mengukur relevansi dan redundansi adalah menggunakan metrik *Mutual Information* (informasi timbal balik). Secara formal, relevansi sebuah fitur $X$ terhadap target $Y$ dihitung dengan persamaan:

$$I(X; Y) = \sum_{y \in Y} \sum_{x \in X} p(x,y) \log \left( \frac{p(x,y)}{p(x)p(y)} \right)$$

Dalam persamaan tersebut, variabel $X$ mewakili nilai fitur, $Y$ adalah variabel target, dan $p(x,y)$ merupakan probabilitas gabungan dari keduanya. Sementara itu, $p(x)$ dan $p(y)$ menunjukkan probabilitas marginal masing-masing. Nilai $I(X; Y)$ yang tinggi menandakan fitur $X$ sangat relevan untuk memprediksi $Y$. Sebaliknya, redundansi yang tinggi terjadi ketika dua buah fitur independen, $X_1$ dan $X_2$, memiliki nilai $I(X_1; X_2)$ yang besar. Seleksi fitur yang ideal akan mencari himpunan dimensi dengan nilai relevansi maksimal dan redundansi minimal.

**Kutukan Dimensi (*Curse of Dimensionality*).**
```{mermaid}
%%| label: fig-ch07-7_2_filter
%%| fig-cap: "Skema Aliran Data Pada Metode Filter"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    subgraph Filter ["Metode Filter"]
        A1[Seluruh Fitur] -->|Uji Korelasi / Informasi Bersama| B1[Subset Fitur]
        B1 --> C1[Model ML]
    end
```
```{mermaid}
%%| label: fig-ch07-7_2_wrapper
%%| fig-cap: "Skema Aliran Data Pada Metode Wrapper"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    subgraph Wrapper ["Metode Wrapper"]
        A2[Kombinasi Fitur] -->|Uji Iteratif Subset| B2[Latih Model]
        B2 -->|Evaluasi Performa| C2{Naik?}
        C2 -->|Ya| A2
    end
```
```{mermaid}
%%| label: fig-ch07-7_2_embedded
%%| fig-cap: "Skema Aliran Data Pada Metode Embedded"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    subgraph Embedded ["Metode Embedded"]
        A3[Seluruh Fitur] -->|LASSO / Regularisasi Koefisien| B3[Proses Pelatihan & Seleksi Menyatu]
    end
```
Motivasi matematis di balik perlunya mereduksi dimensi adalah menghindari fenomena *curse of dimensionality*. Semakin banyak sumbu fitur yang disertakan, volume ruang representasi akan bertambah secara eksponensial. Konsekuensi pertama dari penambahan volume ini adalah timbulnya kerenggangan observasi (*sparsity*); kumpulan sampel yang awalnya padat pada satu atau dua dimensi akan tersebar sangat renggang satu sama lain saat didistribusikan ke dalam ratusan dimensi (Gambar 7.1 memperlihatkan bagaimana penambahan sumbu menyebabkan pola data secara alami saling menjauh). Konsekuensi kedua berdampak pada kebutuhan data pelatihan. Untuk mempertahankan tingkat kepadatan ruang sampel yang setara saat jumlah dimensi diperbanyak, jumlah baris observasi *dataset* pelatihan harus dilipatgandakan secara eksponensial. Konsekuensi terakhir adalah menurunnya daya diskriminatif metrik spasial; kerenggangan di dalam ruang berdimensi tinggi mendistorsi metrik perhitungan algoritma yang bekerja berdasarkan nilai proksimitas jarak, seperti metode *k-Nearest Neighbors* (k-NN).

Di dalam ruang dengan ratusan dimensi, rasio selisih jarak absolut antara tetangga terdekat dan tetangga terjauh menyusut tajam. Seluruh sampel akan terlihat berjarak hampir sama dari titik mana pun yang sedang dievaluasi. Akibatnya, fungsi jarak kehilangan maknanya dan model gagal mengenali kemiripan antar observasi. Seleksi fitur memotong dimensi untuk menjaga kepadatan ruang sehingga komputasi jarak matematis tetap relevan.

## Metode Filter

Pendekatan paling dasar untuk menyeleksi fitur adalah metode filter. Metode ini mengevaluasi kualitas setiap atribut secara mandiri berdasarkan sifat statistiknya, terpisah dari algoritma *machine learning* yang akan digunakan di tahap akhir. Metode ini bertindak sebagai mekanisme penyaring awal (*pre-processing*). Karena eksekusinya tidak memerlukan proses pelatihan model yang membutuhkan komputasi berat, metode *filter* beroperasi sangat cepat dan menjadi pilihan standar untuk memangkas ukuran *dataset* yang berdimensi ekstrem (misalnya puluhan ribu variabel).
```{mermaid}
%%| label: fig-ch07-7_3
%%| fig-cap: "Forward vs Backward Selection Path"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    subgraph Forward ["Seleksi Maju"]
        A1[Kumpulan Kosong] -->|Tambah Fitur Terbaik 1 Per 1| B1[Kumpulan Fitur Optimal]
    end
    subgraph Backward ["Eliminasi Mundur"]
        A2[Kumpulan Lengkap Seluruh Fitur] -->|Buang Fitur Terburuk 1 Per 1| B2[Kumpulan Fitur Optimal]
    end
```
Dalam mengevaluasi kelayakan fitur, metode filter menggunakan metrik yang disesuaikan dengan tipe data fitur dan target:

- **Korelasi Pearson:** Digunakan untuk fitur numerik dan target numerik. Mengukur kekuatan hubungan linear. Fitur dengan skor mendekati 1 atau -1 dipertahankan. Rumus dasarnya mengukur kovarians yang dinormalisasi:
  $$r = \frac{\sum (x_i - \bar{x})(y_i - \bar{y})}{\sqrt{\sum (x_i - \bar{x})^2 \sum (y_i - \bar{y})^2}}$$
  **Kelemahan matematis:** Jika hubungan antara fitur dan target bersifat non-linear (misalnya berbentuk huruf U), korelasi Pearson akan menghasilkan skor mendekati nol, sehingga fitur penting tersebut berisiko terbuang secara keliru.

- ***Mutual Information* (MI):** Teori informasi entropi yang mengatasi kelemahan asumsi linearitas pada Pearson. MI mengukur seberapa besar porsi ketidakpastian target $Y$ berkurang jika kita mengetahui nilai fitur $X$. Semakin besar nilai MI, semakin informatif fitur tersebut:
  $$I(X; Y) = \sum_{y \in Y} \sum_{x \in X} p(x,y) \log \left( \frac{p(x,y)}{p(x)p(y)} \right)$$

- ***Chi-Square* ($\chi^2$):** Diterapkan ketika fitur dan target sama-sama bersifat kategorikal. Uji statistik ini membandingkan frekuensi aktual kategori dengan frekuensi ekspektasi teoretis seandainya fitur dan target sepenuhnya independen:
  $$\chi^2 = \sum \frac{(O_i - E_i)^2}{E_i}$$
  Jika skor $\chi^2$ sangat besar, asumsi independensi gugur, yang berarti fitur kategori tersebut memberikan sinyal kuat untuk kelas target.

- ***Mean(SHAP)* sebagai Filter Modern:** Praktik terbaru mulai mengadopsi nilai rata-rata absolut SHAP (*SHapley Additive exPlanations*) yang diekstraksi dari pohon model sederhana yang dilatih cepat, lalu digunakan sebagai kriteria filter *univariate* statis sebelum data masuk ke *pipeline* utama yang lebih berat.

Meskipun unggul dalam skalabilitas dan kecepatan komputasi, kelemahan mendasar metode filter adalah evaluasinya yang terisolasi. Metode ini mudah melewatkan fitur interaksi (dua fitur yang tampak lemah sendiri namun sangat kuat ketika digabungkan), serta gagal menyaring redundansi (fitur yang skor statistiknya tinggi namun hanya menduplikasi satu sama lain). Keterbatasan inilah yang memotivasi lahirnya metode *wrapper* dan *embedded* yang mengevaluasi himpunan kombinasi fitur secara kolektif.

## Metode Wrapper

Metode *wrapper* memperlakukan seleksi fitur sebagai masalah pencarian yang berpusat pada sebuah algoritma *machine learning*. Berbeda dengan metode filter yang mengevaluasi fitur secara terpisah, metode *wrapper* melatih model prediksi secara langsung pada berbagai susunan fitur dan menggunakan performa model tersebut sebagai indikator kualitas kombinasi fitur.

Algoritma ini jarang beroperasi menggunakan pencarian menyeluruh (*exhaustive search*). Apabila sebuah dataset memiliki $n$ fitur, menguji semua kombinasi menuntut algoritma untuk melatih model sebanyak $2^n$ kali. Secara matematis, pendekatan pencarian ini menargetkan susunan *subset* optimal berikut:

$$\max_{S \subset F} \text{Performa}(M, S)$$

Dalam perumusan tersebut, variabel $F$ melambangkan himpunan seluruh fitur awal, $S$ adalah *subset* fitur yang sedang dievaluasi, $M$ adalah algoritma *machine learning* yang dilatih, dan fungsi $\text{Performa}$ mewakili skor metrik (misalnya akurasi atau RMSE) pada data validasi. Beban komputasi untuk menjalankan model sebanyak $2^n$ iterasi ini sangat berat dan tidak praktis bila diaplikasikan pada *dataset* berdimensi tinggi. Oleh karena itu, metode *wrapper* mengadopsi prosedur heuristik untuk memilah kombinasi *subset* secara bertahap.

Terdapat dua mekanisme lazim dalam penerapan metode *wrapper*, yaitu metode eliminasi rekursif dan seleksi sekuensial. Metode pertama, *Recursive Feature Elimination* (RFE), mengurangi jumlah fitur secara mundur dengan memanfaatkan model algoritma yang memiliki koefisien bobot parameter internal bawaan, seperti model perhitungan regresi linier atau *Support Vector Machine*. Pada putaran tahap awal, metode RFE akan melatih arsitektur model dengan menggunakan seluruh himpunan variabel lalu memetakan skor metrik koefisien di setiap atribut tersebut. Algoritma lantas mengeliminasi fitur yang bernilai absolut bobot terkecil lalu melatih ulang struktur model menggunakan sisa *subset* fitur yang masih dipertahankan. Rotasi pengeliminasian atribut ini berulang terus-menerus hingga total ketersediaan atribut mengecil dan tersisa sejumlah fitur ke batas yang ditargetkan.

Pendekatan kedua adalah *Sequential Feature Selection* (SFS). Berbeda dengan RFE, metode SFS bersifat *model-agnostic* karena tidak bertumpu pada bobot internal model, sehingga mampu membungkus algoritma apa pun. SFS umumnya bergerak melalui salah satu dari dua arah pencarian. Arah pencarian maju (*forward selection*) bermula dari himpunan fitur kosong; algoritma melatih model pada setiap variabel tunggal, memilih satu fitur berkinerja terbaik, lalu secara iteratif memasangkannya dengan fitur-fitur tersisa hingga metrik performa berhenti membaik. Sebaliknya, arah pencarian mundur (*backward elimination*) bermula dari penggabungan seluruh fitur yang tersedia; seleksi bekerja dengan mencopot satu per satu fitur yang kehilangannya menimbulkan kerugian performa paling minimal.
```{mermaid}
%%| label: fig-ch07-7_5
%%| fig-cap: "Alur Validasi Silang Bersarang (Nested Cross-Validation)"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    subgraph Outer_Loop ["Outer Loop - Evaluasi Performa Akhir"]
        A1[Data Utuh] -->|Split| B1[Train Split]
        A1 -->|Split| C1[Test Split]
    end
    subgraph Inner_Loop ["Inner Loop - Seleksi Fitur & Tuning Hyperparameter"]
        B1 -->|Split Fold Baru| A2[Latih Subfold]
        B1 -->|Split Fold Baru| B2[Validasi Subfold]
        A2 -->|Uji Kombinasi Fitur| C2[Tuning & Seleksi]
    end
```
Pemakaian metode *wrapper* menghadirkan profil kinerja komputasi yang kontras dibandingkan metode filter. Keunggulan utama pendekatan ini terletak pada kemampuannya menangkap interaksi fitur yang kompleks; pengujian secara berkelompok memastikan bahwa dua variabel yang tampak tidak relevan saat dievaluasi secara individual dapat diidentifikasi kepentingannya ketika disatukan. Di samping itu, optimalisasi fitur bersifat terikat model, sehingga *subset* yang dihasilkan dipastikan sangat kompatibel dengan algoritma *machine learning* yang mengeksekusi pencarian.

Kendati demikian, implementasi operasional dari prosedur pencarian \*wrapper\* ini memicu dua kendala spesifik yang perlu dievaluasi. Pertama, rotasi iteratif penugasannya memberikan beban komputasi yang tinggi; metode *wrapper* berjalan relatif lambat akibat keharusan melatih ulang banyak siklus model dengan menggunakan variasi formasi parameter atribut. Kedua, proses pencarian iteratif yang terlampau spesifik rentan mendatangkan kemungkinan \*overfitting\*. Prosedur seleksi model saat memaksimalkan nilai metrik skor prediksi bisa menjadikan hasil pilihan *subset* fitur sekadar mengidentifikasi angka kebetulan dari sebaran distribusi \*noise\* sampel himpunan data pelatihan; efek bias dari keteledoran komputasi seleksi ini kerap makin eskalatif dan memburuk tatkala sistem diinstruksikan menyeleksi sebaran partisi variabel *dataset* dengan kapasitas observasi berukuran minim tanpa difilter sistem perlindungan evaluasi berantai.

## Metode Embedded

Metode *embedded* mengintegrasikan kerangka perhitungan penyeleksian matriks fitur agar melekat langsung ke tahap kalibrasi proses pembelajaran model algoritma. Proses evaluasi parameter atribut dilakukan paralel pada tahapan pengoptimalan konvergensi perhitungan bobot model tersebut. Rangkaian mekanisme seleksi terpadu *embedded* mengonsumsi sumber daya komputasi secara jauh lebih minimalis dari skema putaran manual *wrapper* dan lebih merujuk relevansi akurasi nilai akhir hasil prediksi jika dibandingkan operasi metode *filter*.

Contoh klasik metode *embedded* adalah regresi LASSO (*Least Absolute Shrinkage and Selection Operator*). LASSO menerapkan regularisasi L1 dengan menambahkan fungsi penalti absolut ke dalam objektif pelatihan:

$$\min_w \left( \frac{1}{2n_{samples}} ||Xw - y||_2^2 + \alpha ||w||_1 \right)$$

Pada perhitungan komputasi objektif tersebut, komponen metrik $X$ berwujud susunan variabel parameter observasi *dataset*, parameter $y$ memproyeksikan target sasaran *machine learning*, $w$ mewakili perumusan matematis matriks angka skala penugasan model tersebut, sedangkan konstanta pembobot $\alpha$ diimplementasikan mengontrol probabilitas seberapa dominan persentase modifikasi penalti diterapkan. Ruang topologi penalti linear metode L1 dikonstruksikan membentuk geometri sudut ekstrem. Geometri batas dari penalti yang memotong bersiku memaksa probabilitas skor prediksi algoritma mengarah menyudut absolut pada sudut persimpangan. Hal tersebut mengikis rasio penalti fitur dimensi dari variabel yang sekiranya tak cukup berkontribusi nilai prediksi tepat pada angka desimal *nol* (*sparse*).

Seleksi *embedded* juga lazim digunakan pada algoritma berbasis pohon seperti *Random Forest* melalui metrik *feature importance* bawaan, contohnya metrik penurunan ketidakmurnian (*impurity*). Meskipun cepat, metrik bawaan pohon rentan bias terhadap fitur dengan kardinalitas tinggi. Untuk mengatasi hal ini, pustaka *gradient boosting* modern menyediakan alternatif yang lebih kokoh. Pustaka CatBoost, sebagai contoh, memiliki metrik `LossFunctionChange` yang mengevaluasi perbedaan *loss* model ketika suatu fitur diabaikan.

Dalam tahapan eksperimen tingkat lanjut, perancangan ruang fitur mematuhi salah satu dari dua filosofi seleksi *embedded* yang berlawanan. Filosofi pertama berpusat pada ekstraksi himpunan minimal-optimal; pada pendekatan ini, algoritma difokuskan untuk menemukan *subset* fitur sekecil mungkin yang tetap mampu mempertahankan akurasi prediksi tertinggi. Regresi LASSO mewakili prinsip ini lewat kompresi matriks, selaras dengan algoritma MRMR (*Minimum Redundancy Maximum Relevance*) yang secara eksplisit meminimalkan redundansi antar-fitur secara matematis.

Sebaliknya, filosofi kedua memprioritaskan pencarian seluruh fitur relevan (*all-relevant*). Alih-alih mengejar kekompakan data, pendekatan ini bertujuan memetakan setiap sumbu informasi yang membawa sinyal prediktif, termasuk pola non-linier yang samar. Algoritma Boruta, misalnya, beroperasi di bawah payung filosofi ini dengan menginjeksi variabel bayangan (*shadow features*) berupa salinan acak data asli. Melalui uji statistik, Boruta membandingkan fitur orisinal dengan versi bayangannya, lalu menyeleksi seluruh atribut yang terbukti mendistribusikan sinyal lebih valid daripada tebakan acak.

Pemilihan metode bergantung pada tujuan akhir analisis. LASSO atau MRMR dipilih saat model dituntut memiliki jumlah fitur yang ringkas, efisien, dan minimalis. Sebaliknya, Boruta digunakan apabila praktisi ingin memetakan seluruh sinyal prediktif tanpa membuang fitur terkait struktur data yang rumit.

## Stabilitas Seleksi dan Validasi Bersarang

Stabilitas seleksi merupakan metrik yang menentukan apakah algoritma seleksi fitur cukup tangguh untuk digunakan di lingkungan produksi. Konsep ini merujuk pada seberapa konsisten subset fitur yang terpilih ketika metode seleksi diterapkan pada variasi sampel data latih yang berbeda. Algoritma seleksi yang stabil akan mempertahankan himpunan fitur inti yang sama meskipun komposisi data latih mengalami fluktuasi akibat proses sampling.

Ketidakstabilan seleksi sering muncul pada himpunan data berdimensi tinggi, terutama ketika jumlah observasi sangat kecil dibandingkan jumlah atribut. Pada kondisi ini, variasi kecil pada data memicu algoritma untuk merombak total daftar pilihan fiturnya. Untuk mengukur stabilitas pilihan antar beberapa iterasi pelipatan data, kita dapat menggunakan rasio kesamaan seperti Indeks Jaccard:

$$J(A, B) = \frac{|A \cap B|}{|A \cup B|}$$

Dalam persamaan tersebut, variabel $A$ mewakili himpunan fitur yang terpilih dari porsi data latih pertama, sedangkan $B$ mewakili himpunan fitur yang terpilih dari porsi data latih kedua. Nilai metrik $J$ yang mendekati angka 1 menunjukkan tingkat stabilitas seleksi yang tinggi. Sebaliknya, nilai yang mendekati 0 mengindikasikan bahwa pilihan fitur sangat sensitif terhadap perubahan sampel data.

Selain stabilitas metrik fitur, kesalahan algoritmis metodologis berisiko dalam urutan parameter seleksi fitur mendatangkan efek kerusakan serius, di mana model gagal membendung indikasi intrusi informasi anomali (*data leakage*). Kekeliruan desain berakibat dari komputasi algoritma pemilahan data dan seleksi atribut (*feature selection*) yang dipaksakan diterapkan mendahului tahap penugasan rotasi observasi untuk blok himpunan komputasi kelompok evaluasi dari pelatihan (*training split*) serta blok pemisah untuk proses pengetesan sampel (*test data*). Dampak teknis dari rotasi penyelesaian kerangka yang membaurkan informasi fitur di blok distribusi \*dataset\* sangat merugikan:

- **Estimasi optimis semu:** Metode seleksi menggunakan distribusi target dari data uji untuk mencari fitur dengan korelasi terkuat.

- **Kehilangan objektivitas:** Model pada dasarnya telah "melihat" sebagian representasi label data uji sebelum fase pelatihan dimulai.

- **Kegagalan saat inferensi:** Performa model saat pengujian silang (*cross-validation*) terlihat meyakinkan, namun akan menurun drastis saat menghadapi data riil yang baru.

Satu-satunya prosedur pencegahan indikasi *data leakage* adalah menggunakan pendekatan komputasi fungsionalisasi prapemrosesan observasi dan implementasi kerangka rancangan arsitektur terintegrasi, yang akrab dipandu melalui instruksi konsep *pipeline* parameter yang tertata benar. Prosedur pencoretan nilai dimensi *feature* mutlak dikomandoi dari kalkulasi varians statistik di struktur sampel matriks pelatihan saja, maka modul kelas penyeleksi wajib dibungkus kokoh berurutan di dalam objek pelaksana alur kerja model (*pipeline*). Logika penyusunan komputasi seleksi wajib ditugaskan pada mode pelatihan kalkulasi nilai eksklusif *fit* hanya mengeksekusi observasi blok pemisahan uji coba komputasi (*training split*), selanjutnya sesudah terkunci model menyortir dan menyesuaikan *dataset* dengan fitur *transform* lurus mengeksekusi kerangka pemecahan porsi blok kelompok dimensi data masa uji.

Kebutuhan akan disiplin pemisahan data ini menjadi jauh lebih kompleks ketika metode seleksi menuntut pencarian hyperparameter, seperti mencari jumlah fitur terbaik ($k$) atau menentukan besaran penalti pada regularisasi L1. Memilih parameter optimal pada validasi silang standar akan memicu bias. Solusi untuk mempertahankan objektivitas evaluasi adalah dengan arsitektur *nested cross-validation* (validasi silang bersarang).

Secara operasional, mekanisme validasi bersarang memisahkan alur pencarian dan pengujian menjadi dua lapis pelipatan yang independen. Lapisan pertama berwujud perulangan dalam (*inner loop*) yang secara eksklusif memecah *subset* data latih demi mencari kombinasi parameter seleksi terbaik, contohnya saat menentukan batas nilai $k$ paling optimal. Setelah parameter ideal ditemukan, lapisan kedua berupa perulangan luar (*outer loop*) mengambil alih proses evaluasi. Perulangan luar ini bertugas mengeksekusi *pipeline* pemodelan secara utuh---termasuk langkah *transform* yang mengadopsi parameter dari putaran dalam---langsung di atas lipatan data uji yang sama sekali belum tersentuh observasi sebelumnya.

Pemisahan struktural ini menjamin estimasi akurasi akhir merepresentasikan kemampuan generalisasi yang realistis, tanpa kontaminasi sekecil apa pun dari tahapan seleksi fitur.

## Studi Kasus: Membandingkan Metode Seleksi pada Satu Dataset

Kita menggunakan satu *dataset* klasifikasi berdimensi tinggi untuk melihat cara kerja metode *filter*, *wrapper*, dan *embedded* secara langsung. Data sintetis ini dikonfigurasi memiliki 1.000 dimensi yang terpecah ke dalam tiga klasifikasi spesifik. Matriks awal memuat 10 fitur inti pembawa sinyal prediktif utama, yang secara mendampingi diikuti oleh 100 fitur redundan sebagai salinan dengan derajat korelasi teramat tinggi terhadap sinyal asli. Di luar kelompok observasi berharga tersebut, sisa matriks disesaki oleh 890 fitur *noise*, yakni variabel buatan serampangan yang sepenuhnya hampa korelasi terhadap vektor target.

Tujuan seleksi adalah mencari *subset* fitur sekecil mungkin tanpa menurunkan akurasi prediksi, sekaligus menjaga efisiensi komputasi.

``` {.python language="Python"}
import time
from sklearn.datasets import make_classification
from sklearn.feature_selection import SelectKBest, mutual_info_classif, RFE, SelectFromModel
from sklearn.linear_model import LogisticRegression

# 1. Membangun data sintetis berdimensi tinggi (100 fitur untuk efisiensi eksekusi)
# Terdiri dari fitur informatif (sinyal inti), redundan, dan noise acak
X, y = make_classification(
    n_samples=500, 
    n_features=100, 
    n_informative=10, 
    n_redundant=20, 
    random_state=42
)

# --- 1. METODE FILTER: Mutual Information (K-Best) ---
start_filter = time.time()
filter_selector = SelectKBest(score_func=mutual_info_classif, k=15)
X_filter = filter_selector.fit_transform(X, y)
time_filter = time.time() - start_filter
selected_filter = filter_selector.get_support(indices=True)

# --- 2. METODE WRAPPER: Recursive Feature Elimination (RFE) ---
# Menggunakan Logistic Regression sebagai estimator pengevaluasi fitur secara kolektif
start_wrapper = time.time()
estimator = LogisticRegression(max_iter=1000, random_state=42)
wrapper_selector = RFE(estimator=estimator, n_features_to_select=15, step=5)
X_wrapper = wrapper_selector.fit_transform(X, y)
time_wrapper = time.time() - start_wrapper
selected_wrapper = wrapper_selector.get_support(indices=True)

# --- 3. METODE EMBEDDED: LASSO L1 (SelectFromModel) ---
# Penalti L1 secara inheren memotong koefisien redundan/noise menjadi nol
start_embedded = time.time()
lasso = LogisticRegression(penalty='l1', solver='liblinear', C=0.1, random_state=42)
embedded_selector = SelectFromModel(estimator=lasso, max_features=15)
X_embedded = embedded_selector.fit_transform(X, y)
time_embedded = time.time() - start_embedded
selected_embedded = embedded_selector.get_support(indices=True)

# Menampilkan perbandingan performa metode seleksi
print("1. Metode Filter (Mutual Information):")
print(f"   - Fitur Terpilih (Indeks): {selected_filter[:10]}...")
print(f"   - Waktu Eksekusi: {time_filter:.4f} detik\n")

print("2. Metode Wrapper (RFE):")
print(f"   - Fitur Terpilih (Indeks): {selected_wrapper[:10]}...")
print(f"   - Waktu Eksekusi: {time_wrapper:.4f} detik\n")

print("3. Metode Embedded (LASSO L1):")
print(f"   - Fitur Terpilih (Indeks): {selected_embedded[:10]}...")
print(f"   - Waktu Eksekusi: {time_embedded:.4f} detik\n")
```

Setiap pendekatan menunjukkan karakteristik kinerja yang berbeda saat dihadapkan pada kombinasi sinyal, redundansi, dan *noise*:

**1. Metode Filter (Mutual Information)**

Algoritma ini menilai setiap atribut secara independen terhadap target. Keunggulan utamanya adalah eksekusi yang sangat cepat; metode ini mampu menyingkirkan 890 fitur *noise* dalam hitungan detik karena skor informasinya mendekati nol. Namun, kelemahan terbesarnya adalah kegagalan dalam menangani redundansi. Karena dievaluasi satu per satu tanpa melihat atribut lain, 100 fitur redundan mendapat skor tinggi dan tetap lolos seleksi bersama 10 fitur inti. Hasil akhirnya adalah ukuran *subset* yang masih terlalu besar.

**2. Metode Wrapper (Recursive Feature Elimination / RFE)**

RFE mengevaluasi kumpulan fitur secara kolektif dengan melatih ulang model secara berulang. Pendekatan ini sangat efektif untuk menyingkirkan redundansi. Ketika satu fitur sudah berhasil mewakili sebuah sinyal prediktif, fitur kembarannya seketika menjadi tidak relevan dan terbuang pada putaran berikutnya, sehingga RFE sukses menemukan *subset* fitur yang paling ringkas. Kendala utamanya adalah biaya komputasi yang mahal, mengingat proses pelatihan ulang model hingga ratusan kali membuat metode ini berjalan sangat lambat.

**3. Metode Embedded (Regresi LASSO)**
Metode *embedded* menyatukan pelatihan model dan seleksi fitur melalui penalti L1. Fungsi objektif LASSO dirumuskan sebagai berikut:

$$\min_{w} \frac{1}{2n} ||Xw - y||_2^2 + \alpha ||w||_1$$

Pada persamaan tersebut, $n$ adalah jumlah sampel, $X$ mewakili matriks fitur, $y$ adalah target, $w$ adalah vektor bobot model, dan $\alpha$ adalah parameter pengendali kekuatan penalti. Suku pertama mengukur seberapa baik model menyesuaikan data, sedangkan suku kedua ($\alpha ||w||_1$) memaksa model menggunakan sesedikit mungkin fitur. Saat nilai $\alpha$ dinaikkan, model menekan koefisien dari atribut redundan dan *noise* hingga tepat menjadi nol.

\[GAMBAR 7.6: Plot Jalur - Pergerakan koefisien fitur (shrinkage path) pada regresi LASSO seiring peningkatan nilai penalti L1, menunjukkan koefisien fitur redundan yang ditekan menjadi nol\]

Pendekatan *embedded* ini memberikan jalan tengah yang optimal. LASSO mampu membuang fitur *noise* dan meminimalkan fitur redundan dengan akurasi yang sebanding dengan RFE, sekaligus menyelesaikan komputasi hampir secepat metode *filter*.

Berdasarkan perbandingan profil komputasi ketiga instrumen tersebut, proses pemilihan algoritma seleksi jelas bergantung pada prioritas alur kerja eksperimen. Metode *filter* sangat ideal diposisikan sebagai saringan tahap pertama pada data berdimensi ekstrem demi menekan fitur *noise* dengan segera. Di lain skenario, metode *wrapper* patut diadopsi jika tujuan spesifik eksperimen adalah mengekstraksi ukuran *subset* sekecil mungkin yang memenuhi kriteria minimal-optimal, dengan catatan bahwa infrastruktur pemrosesan sanggup memfasilitasi beban pelatihan model iteratif. Sebagai konklusi praktis, penerapan metode *embedded* merupakan jalan tengah paling direkomendasikan untuk skenario pemodelan pada umumnya, mengingat teknik ini merumuskan titik temu paling stabil antara kapabilitas penyingkiran redundansi dan rasionalitas efisiensi memori.
