# Representasi Fitur Kategorikal

Data kategorikal sering kali mewakili entitas non-numerik yang sangat kaya informasi, seperti nama kota, jenis produk, atau tingkat kepuasan pelanggan. Namun, karena algoritma pemelajaran mesin beroperasi murni pada ruang vektor matematis, label teks mentah ini tidak dapat dicerna secara langsung. Tantangan utama dalam merepresentasikan fitur kategorikal adalah bagaimana memetakan label kualitatif ke dalam format kuantitatif tanpa memaksakan asumsi keliru, seperti memberikan bias urutan buatan pada variabel nominal, serta tanpa memicu penambahan dimensi matriks yang dapat menurunkan efisiensi komputasi.

Bab ini membahas taksonomi penanganan data kategorikal dari pendekatan klasik hingga representasi neural mendalam (*deep learning*). Pembahasan mencakup mekanisme dasar *one-hot* dan *ordinal encoding*, penanganan fitur berkardinalitas tinggi menggunakan *frequency* dan *target encoding*, pencegahan *data leakage*, efisiensi memori melalui *hashing trick*, hingga pemetaan semantik melalui *entity embedding*. Di akhir bab, kita merancang ketangguhan sistem dalam menangani kemunculan kelas baru (*unseen categories*) saat tahap inferensi produksi.

## Jenis-Jenis Variabel Kategorikal

Model *machine learning* tidak dapat memproses label teks mentah secara langsung karena algoritma beroperasi pada ruang vektor matematis. Proses penerjemahan label ini ke dalam representasi numerik disebut *encoding*, yang secara esensial memetakan sebuah himpunan kategori (*vocabulary*) ke dimensi bilangan riil:
$$f_{\text{encode}} : \mathcal{V} \rightarrow \mathbb{R}^d$$
Di mana $\mathcal{V}$ melambangkan himpunan unik kategori (misalnya $\{\text{"Merah"}, \text{"Biru"}\}$), dan $d$ merupakan dimensi vektor keluaran (contohnya $d=1$ untuk *Ordinal Encoding*, atau $d=|\mathcal{V}|$ untuk *One-Hot Encoding*).

n dangkal untuk kategori yang langka.
```{mermaid}
%%| label: fig-ch04-4_1
%%| fig-cap: "Dampak Kardinalitas Tinggi pada Percabangan dan Overfitting Model Pohon"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    A[Simpul Akar: Kategori Kardinalitas Tinggi - e.g. ID Kota] --> B[Cabang Kota A]
    A --> C[Cabang Kota B]
    A --> D[Cabang Kota C]
    A --> E[Cabang Kota... N - Pohon Terlalu Lebar & Dangkal]
```
Satu nuansa teknis yang penting dalam perancangan *pipeline* adalah penanganan nilai kosong (*missing values*) seperti `np.nan` atau `None`. Dalam ekosistem produksi modern seperti Scikit-Learn versi 1.9, nilai kosong tidak selalu harus dibuang atau diimputasi secara terpisah. Kerangka kerja tersebut memungkinkan penanganan nilai kosong sebagai entitas kategori mandiri secara langsung di dalam kelas *encoder*, misalnya menggunakan parameter `encoded_missing_value` pada *OrdinalEncoder* untuk memetakan nilai `NaN` ke angka *integer* khusus secara deterministik. Pemahaman akan kelas kardinalitas dan perlakuan khusus terhadap entitas kosong ini menetapkan fondasi bagi teknik transformasi yang akan diaplikasikan.

## *One-Hot* dan *Ordinal Encoding*

Model *machine learning* hanya dapat memproses format numerik. Data kategorikal wajib diterjemahkan menjadi angka sebelum masuk ke tahap pelatihan. Dua pendekatan dasar untuk konversi ini adalah *ordinal encoding* dan *one-hot encoding*. Pemilihan metode bergantung pada keluarga model yang dipakai serta struktur hierarki bawaan dari fitur tersebut.

***Ordinal Encoding*.**

*Ordinal encoding* memetakan setiap kategori menjadi bilangan bulat berurutan. Misalnya, fitur ukuran baju dengan tingkatan alami "Kecil", "Sedang", dan "Besar" diubah secara berurutan menjadi angka 1, 2, dan 3.

Implementasi *ordinal encoding* berfungsi dengan sangat baik pada algoritma berbasis pohon. Model seperti *decision tree* dan *random forest* memproses angka-angka ini murni sebagai penanda kategori untuk batas pembelahan cabang, tanpa menyimpulkan bahwa ada jarak matematis absolut antar-angka tersebut. Sebaliknya, metode ini sangat keliru jika digunakan pada model linier untuk data nominal. Jika kita memetakan kategori kota yang tanpa urutan alami (misalnya "Jakarta" menjadi 1, "Bandung" menjadi 2, dan "Surabaya" menjadi 3), model linier akan membaca rentang angka tersebut secara proporsional. Akibatnya, model menyimpulkan secara keliru bahwa "Surabaya" bernilai tiga kali lipat dari "Jakarta". Pada ekosistem yang lebih praktis, pustaka modern menyediakan utilitas parameter untuk mengalokasikan data kosong atau kategori tak dikenal (yang baru muncul saat inferensi) ke angka spesifik seperti -1, sehingga menjaga *pipeline* produksi tetap berjalan tanpa *error*.

***One-Hot Encoding*.**

Untuk mewakili data nominal tanpa memaksakan urutan palsu, data direpresentasikan ulang menggunakan *one-hot encoding*. Metode ini memecah satu kolom kategori menjadi beberapa kolom fitur biner terpisah.

Transformasi kategorinya dirumuskan dalam bentuk vektor:

$$\mathbf{x} = [x_1, x_2, \dots, x_K] \in \{0, 1\}^K$$

Di mana $K$ melambangkan total kategori unik pada fitur tersebut. Elemen $x_i$ bernilai $1$ apabila observasi termasuk dalam kategori ke-$i$, dan $0$ untuk seluruh kategori sisanya. Sebagai contoh, atribut data berisi opsi warna "Merah", "Biru", dan "Hijau" dipetakan sedemikian rupa sehingga pengamatan warna "Merah" direpresentasikan menjadi vektor $[1, 0, 0]$.

Karakteristik utama *one-hot encoding* adalah kemampuannya menghilangkan bias urutan, karena setiap opsi kategori terisolasi dalam kolom independen dan dihitung oleh model linier secara terpisah. Namun, pendekatan ini memiliki kekurangan mendasar pada ekspansi dimensi matriks. Untuk fitur berkardinalitas tinggi dengan ratusan elemen unik, pelebaran dimensi terjadi secara tajam dan menghasilkan matriks dengan mayoritas angka nol (*sparse matrix*), sehingga membutuhkan kapasitas memori komputasi yang sangat besar. Oleh karena itu, pelebaran matriks ini biasanya dikendalikan dengan menetapkan batasan frekuensi kemunculan minimum. Kategori yang jarang muncul akan digabungkan ke satu kolom sisa (*infrequent bucket*), yang juga dirancang untuk menampung label baru saat tahap inferensi.

## Frequency dan Count Encoding

Fitur dengan kardinalitas tinggi menyebabkan representasi biner membutuhkan terlalu banyak memori akibat dimensi matriks yang membesar. *Frequency encoding* dan *count encoding* menawarkan pendekatan kompresi dengan mengubah label kategori menggunakan metrik distribusinya, sehingga menghindari penambahan kolom matriks.

Mekanisme kompresinya bekerja melalui dua cara utama. Pada *count encoding*, label kategori diubah menjadi angka frekuensi absolut kemunculannya di dalam *dataset* latih. Sebagai contoh, atribut nama kota diganti langsung dengan angka total kemunculan transaksi dari kota tersebut. Sementara itu, pada *frequency encoding*, label kategori direpresentasikan melalui nilai proporsi kemunculannya terhadap total baris data.

Kalkulasi proporsi pada *frequency encoding* untuk sebuah kategori $c$ didefinisikan melalui persamaan $f(c) = \frac{N(c)}{N_{total}}$. Pada rumusan tersebut, variabel $N(c)$ mewakili jumlah observasi kategori $c$ di dalam matriks latih, dan $N_{total}$ menunjukkan ukuran baris keseluruhan dari populasi latih.
```{mermaid}
%%| label: fig-ch04-4_3
%%| fig-cap: "Pemetaan Count Encoding Berdasarkan Jumlah Observasi"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph LR
    A[Kolom Mentah: ID_Provinsi] -->|Kalkulasi Kemunculan Kategori| B[Tabel Distribusi Frekuensi]
    B --> C[Kolom Numerik Baru: Total Jumlah Transaksi Per Provinsi]
```
Teknik ini memadatkan ribuan kategori ke dalam satu kolom numerik tunggal. Kompresi ini efektif karena tingkat frekuensi kemunculan atribut sering kali memiliki korelasi langsung dengan variabel target. Kota dengan volume pesanan harian yang besar biasanya memiliki profil demografi atau logistik yang berbeda dari kota dengan transaksi sporadis. Konversi ini memberi model representasi skala untuk mengevaluasi suatu entitas.

Pendekatan berbasis hitungan ini sangat efisien, namun memunculkan dua kerentanan teknis saat diterapkan ke fase produksi. Kerentanan pertama adalah tabrakan probabilitas (*collision*). Dua observasi kategori berbeda yang memiliki riwayat jumlah kemunculan sama persis akan diubah menjadi nilai parameter yang sama. Bila Kota Tegal dan Kota Blitar sama-sama muncul sebanyak 150 kali, algoritma tidak mampu lagi membedakan representasi kedua kota tersebut. Kerentanan kedua muncul saat menghadapi kategori belum terlihat (*unseen categories*). Entitas yang baru tercatat pertama kali pada fase inferensi tidak memiliki rekam perhitungan statistik dari data latih, sehingga berisiko memunculkan galat pemrosesan.

Pustaka prapemrosesan modern menyediakan parameter bawaan untuk memitigasi celah tersebut. Untuk mengatasi tabrakan, pustaka seperti `category_encoders` memiliki fasilitas penggabungan label minoritas. Fitur ini menyatukan kategori berfrekuensi rendah ke dalam satu kelompok sebelum dihitung proporsinya, sehingga mereduksi potensi tabrakan nilai acak antar-entitas langka. Untuk penanganan nilai inferensi baru, kelas transformasi umumnya menggunakan parameter yang secara otomatis memberikan skor batas nol pada label kategori yang tidak pernah diobservasi selama pelatihan model. Distribusi kategori awal wajib diperiksa oleh praktisi demi memvalidasi bahwa tingkat tabrakan nilai akibat transformasi ini tidak mengorbankan variansi penting dalam *dataset*.

## *Target Encoding* dan Risiko *Leakage*

Kardinalitas tinggi pada variabel kategori memicu pelebaran dimensi matriks secara masif jika dikonversi menggunakan metode representasi matriks renggang seperti *one-hot encoding*. Sebagai solusi, *target encoding* (atau *mean encoding*) muncul sebagai teknik alternatif yang memampatkan berbagai label kategori menjadi satu lajur numerik kontinu. Representasi ini dibentuk dengan mengganti setiap string kategori menggunakan nilai rata-rata probabilitas target dari sekumpulan observasi yang memiliki label tersebut.

Kelompok kategori minoritas dengan sampel yang sangat sedikit rentan memunculkan rata-rata yang fluktuatif (bervariansi tinggi). Implementasi operasional meredam anomali ini menggunakan metode pemulusan (*smoothing*). Arsitektur pemulusan berbasis *empirical Bayes shrinkage* memadukan probabilitas kelompok spesifik bersama rata-rata distribusi populasi umum melalui persamaan $S_i = \lambda_i \mu_i + (1 - \lambda_i) \mu_{global}$.

Dalam formula tersebut, $S_i$ adalah nilai numerik terenkode yang merepresentasikan kategori $i$, $\mu_i$ adalah rata-rata nilai target lokal pada kelompok label $i$, dan $\mu_{global}$ adalah rata-rata nilai target keseluruhan berdasarkan data latih. Rasio penyeimbang probabilitas diatur oleh pembobot $\lambda_i$, yang diformulasikan berdasarkan jumlah sampel melalui persamaan $\lambda_i = \frac{n_i}{n_i + m}$. Di sini, $n_i$ merupakan angka frekuensi observasi kategori $i$, dan $m$ adalah parameter regulasi untuk menarik variansi sampel menuju rata-rata sistem. Ketika frekuensi kelas sangat padat ($n_i \gg m$), nilai batas $\lambda_i$ beranjak mengikat mendekati angka 1, sehingga sistem condong meyakini rata-rata kelompok lokalnya. Sebaliknya, jika kategori berstatus langka, dominasi $\lambda_i$ menciut dan model meregulasi komputasinya mendekat ke angka landasan populasi global.

**Mekanisme Pemblokiran *Data Leakage*.**

Penggunaan distribusi probabilitas dari variabel sasaran (target) untuk menyusun parameter prediksi membawa risiko *data leakage*. Apabila perhitungan rata-rata ini melibatkan observasi dari seluruh set data, proses pelatihan pada dasarnya menyerap informasi yang seharusnya menjadi bagian dari pengujian (*test set*). Hasilnya adalah metrik evaluasi yang menyesatkan dan tingkat akurasi yang tidak valid saat model beroperasi di lingkungan produksi.

Kedisiplinan arsitektur diimplementasikan pada pustaka modern untuk memblokir *data leakage* melalui tiga metode komputasi utama. Pertama, pemisahan himpunan terisolasi. Probabilitas setiap kategori diukur murni dari himpunan latih tanpa menyertakan variabel lain, kemudian disimpan sebagai acuan oleh kelas *encoder*. Saat metode *transform* memproses data asing di fase inferensi, parameter menggunakan angka rata-rata populasi global ($\mu_{global}$). Kedua, penerapan validasi silang internal (*cross-fitting*). Implementasi struktur seperti `TargetEncoder` pada pustaka *Scikit-Learn* secara internal memecah data latih menjadi kumpulan sub-lipatan (*folds*). Rata-rata target untuk sampel di dalam sebuah lipatan tertentu dihitung eksklusif menggunakan observasi target dari sisa lipatan yang ada, sehingga data tidak dievaluasi menggunakan rekam jejak dirinya sendiri. Ketiga, metode statistik target terurut (*ordered target statistics*). Algoritma model pohon seperti CatBoost menekan potensi *data leakage* melalui skema penyusunan urutan acak. Estimasi rasio target pada sebuah baris matriks hanya memformulasikan data sampel pengamatan historis yang memiliki peringkat \"lebih awal\" dari observasi baris itu sendiri. Regulasi perhitungan satu arah ini berhasil memutus siklus pemanfaatan parameter target tanpa membutuhkan perhitungan permutasi antar-lipatan *folds* secara berlebih.
```{mermaid}
%%| label: fig-ch04-4_4
%%| fig-cap: "Skema Validasi Silang (Cross-Fitting) untuk Target Encoding"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    A[Dataset Latih] -->|Bagi Dua Folds| B[Fold Latih 1]
    A -->|Bagi Dua Folds| C[Fold Latih 2]
    B -->|Hitung Mean Target| D[Tabel Target Enc 1]
    C -->|Hitung Mean Target| E[Tabel Target Enc 2]
    D -->|Terapkan Pengkodean| C
    E -->|Terapkan Pengkodean| B
```
## *Hashing* untuk Fitur Berkardinalitas Tinggi

Fitur berisikan rincian ratusan atau jutaan representasi elemen kategori tunggal, seperti nomor ID transaksi dan catatan riwayat alamat jaringan (IP), menyebabkan penambahan dimensi secara eksponensial apabila algoritma dipaksa menggunakan prapemrosesan vektor *one-hot*. Implementasi konvensional algoritma menuntut sistem memelihara referensi kosakata yang berkesinambungan bagi tiap tipe kelas entitas kategori. Penambahan akumulasi informasi data matriks seiring waktu mengharuskan ukuran penyimpanan parameter berkembang pesat hingga mendominasi batas performa komputasi infrastruktur.

Tanpa perlu memelihara kamus referensi, *feature hashing* (atau *hashing trick*) menerapkan fungsi matematis secara langsung pada teks kategori untuk menghasilkan angka indeks yang tetap. Teks diubah menjadi vektor numerik dengan lebar lajur yang dipatok statis sejak awal. Operasi ini berpusat pada kalkulasi *modulo* terhadap hasil *hash*. Posisi indeks kolom untuk sebuah kategori $c$ ditentukan oleh persamaan:

$$\text{indeks}(c) = \text{hash}(c) \pmod{k}$$

Persamaan tersebut mendeskripsikan ($\text{hash}(c)$) sebagai skor akhir perhitungan modul pencarian fungsi *hash* (umumnya memanfaatkan metode skema kalkulasi MurmurHash3), sementara operator parameter ($k$) beroperasi sebagai batasan dimensi total atribut (\*n_components\*). Melalui batasan ukuran ini, ruang komputasi memori tidak diharuskan menyimpan tabel struktur referensi masa lalu. Teks masukan dalam skema apa pun dimodifikasi serempak menjadi variabel indeks terstruktur tanpa memedulikan observasi varians masa depan yang belum ditemukan.
```{mermaid}
%%| label: fig-ch04-4_5
%%| fig-cap: "Ilustrasi Mekanisme Hashing Trick dan Terjadinya Hash Collision"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    A1[Kategori: Jakarta] -->|Fungsi Hash MD5/Murmur| B[Nilai Hash Numerik]
    A2[Kategori: Bandung] -->|Fungsi Hash MD5/Murmur| B
    B -->|Modulo N=10| C{Indeks Matriks Fitur}
    C -->|Kolisi Terjadi| D[Satu Indeks Fitur Yang Sama]
```
Pengendalian kapasitas melalui penentuan ruang berakibat pada konsekuensi teknis di dalam wujud kerentanan tabrakan fungsi metrik (*collision*). Matriks dimensi vektor luaran ($k$) ditentukan secara spesifik menjadi lajur sempit dibandingkan sebaran kemungkinan fitur yang hampir tidak terbatas. Kompresi ini mengakibatkan dua kategori berbeda dapat diarahkan dan diwakilkan menjadi nomor titik indeks kolom yang sama. Sistem mengintegrasikan kedua objek yang saling terisolasi dalam kalkulasi pengamatan matriks sebagai kesatuan yang seragam, sehingga bobot nilai korelasinya akan bercampur dan bias.

Untuk meredam bias dari tumpukan benturan ini, pustaka `scikit-learn` melalui modul `FeatureHasher` menerapkan mekanisme *alternate sign*. Komputasi memanfaatkan fungsi *hash* kedua untuk mendistribusikan nilai secara asimetris. Ketika banyak kategori bertabrakan pada satu indeks, nilainya dijumlahkan dengan tanda positif dan negatif yang berselang-seling. Secara ekspektasi matematis, derau dari label yang saling menumpuk akan menetralkan satu sama lain.

Secara operasional, penggunaan teknik *hashing* menuntut penyesuaian ekspektasi pada pengembangan model. Di satu sisi, keandalan operasional dan stabilitas komputasi tetap terjaga di hadapan serbuan label tak terduga, meski ketajaman batas keputusan pudar sebanding dengan besarnya proporsi benturan. Efisiensi komputasi dari pemrosesan data yang berat pun menjadi jauh lebih ringan berkat dukungan eksekusi paralel dan pemecahan matriks, sebagaimana yang diimplementasikan oleh `HashingEncoder` dalam pustaka `category_encoders`. Namun, di sisi lain, semua ini dibayar dengan hilangnya interpretabilitas secara total. Kejelasan representasi hilang karena lajur fitur berubah menjadi kotak hitam matematis; satu kolom spesifik tidak lagi mewakili konteks fitur asli apa pun. Kondisi inilah yang menjadi alasan mengapa sebagian perpustakaan analitis secara eksplisit menolak menyertakan modul *hashing* ke dalam alat prapemrosesan mereka.

## *Entity Embedding*

Transformasi kategori konvensional seperti *one-hot encoding* menghasilkan matriks biner yang lebar, renggang, dan mengabaikan kemiripan semantik antar-kategori. Pendekatan *entity embedding* menyelesaikan masalah kardinalitas ini dengan menempatkan setiap tingkat kategori ke dalam ruang vektor padat berdimensi rendah. Nilai di dalam vektor merupakan bobot parameter yang terus diperbarui sepanjang proses pelatihan jaringan saraf (Guo & Berkhahn, 2016).

Matriks *embedding* bekerja sebagai sebuah tabel pencarian (*lookup table*). Pemetaan dari ruang kategori menuju koordinat kontinu dirumuskan melalui perkalian antara vektor *one-hot* dengan matriks bobot:

$$\mathbf{e}_i = \mathbf{W} \mathbf{x}_i$$

Penjelasan notasi:

- $\mathbf{e}_i \in \mathbb{R}^d$ adalah representasi vektor padat berdimensi $d$ untuk kategori ke-$i$.

- $\mathbf{W} \in \mathbb{R}^{d \times |V|}$ merupakan matriks *embedding* yang berisi bobot parameter jaringan saraf, dengan $|V|$ sebagai total kardinalitas unik.

- $\mathbf{x}_i \in \{0,1\}^{|V|}$ menunjukkan bentuk *one-hot* dari kategori tersebut.
```{mermaid}
%%| label: fig-ch04-4_6
%%| fig-cap: "Proses Pemetaan Indeks Kategori Berformat One-Hot Menjadi Vektor Kontinu Berdimensi Rendah"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph LR
    A[Kategori One-Hot: 0, 1, 0, 0] -->|Lookup Lapisan Bobot| B(Layer Kerapatan / Embedding)
    B --> C[Vektor Representasi Padat Laten: e.g. 0.15, -0.42]
```
Pelatihan matriks $\mathbf{W}$ berintegrasi langsung dengan pembaruan parameter model utama. Berdasarkan fungsi kerugian (*loss function*), arsitektur saraf akan menyesuaikan koordinat geometri dari setiap atribut. Pembaruan matriks ini menanamkan tiga karakteristik penting pada fitur kategorikal. Pertama, terjadi penyusutan dimensi di mana struktur kontinu menghilangkan lonjakan variabel renggang yang memicu masalah *overfitting*, terutama pada basis data dengan ribuan tingkat kardinalitas. Kedua, terbentuk pemetaan kedekatan semantik. Kategori yang berbagi distribusi target ekuivalen secara otomatis akan diletakkan berdekatan dalam ruang Euclidean; misalnya, vektor menempatkan "Sabtu" dan "Minggu" secara berdampingan jika keduanya menampilkan pola akhir pekan yang identik. Terakhir, *embedding* memberikan portabilitas fitur; nilai akhir pemetaan ini dapat diekstrak menjadi deretan fitur numerik statis, sehingga model tradisional seperti *gradient boosting* dapat menikmati representasi vektor padat tanpa harus mengeksekusi jaringan saraf saat tahapan produksi.

Aplikasi vektor padat kini melibatkan arsitektur bahasa yang terlatih penuh (*pretrained language models*). Bakumenko et al. (2024) membuktikan pemrosesan teks dari nama label kategorikal non-linguistik, seperti ID produk atau kode pos, mampu berubah menjadi himpunan vektor semantik yang meningkatkan kinerja prediktif model. Desain rekomendasi terpadu (Lei et al., 2026) turut mengintegrasikan *embedding* kategorikal sebagai satu modalitas utama yang setara dengan pemrosesan teks dan citra. Pendekatan pemetaan parameter ini merintis transisi awal teknik rekayasa data menuju pemanfaatan struktur model arsitektur dalam, tanpa menuntut intervensi logika transformasi dari pengguna.

## Menangani Kategori Belum Terlihat Saat *Inference*

Data pelatihan adalah potret statis dari kejadian masa lalu. Di lingkungan produksi, model sering kali menghadapi perubahan kondisi yang memunculkan entitas baru. Kategori belum terlihat (*unseen categories*) merupakan kelas atau label pada fitur kategorikal yang baru muncul saat model dievaluasi di lapangan dan sama sekali tidak ada di dalam himpunan data latih. Contoh nyatanya adalah kemunculan merek gawai rintisan baru atau kode pos dari wilayah pemekaran.

Kehadiran kategori baru yang tidak tercatat dalam tahap awal komputasi menciptakan ketidaksesuaian struktur dimensi pada *pipeline* operasional. Metode klasifikasi nominal dasar *one-hot encoding* menentukan posisi pemetaan vektor mengacu kepada ketersediaan kategori referensi saat memproses parameter *fit*. Apabila kerangka pemetaan *encoder* menemukan masukan label matriks tak teridentifikasi saat skema metode *transform* beroperasi, pemetaan fungsi rentang matriks menjadi tidak ekuivalen. Struktur komputasi gagal memproses representasi ini dan menghasilkan galat kesalahan (*error*) tipe data yang menghentikan rangkaian fungsi program.
```{mermaid}
%%| label: fig-ch04-4_7
%%| fig-cap: "Mekanisme Pipeline Menangani Kategori Belum Terlihat Melalui Pengelompokan Proaktif dan Fallback"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    A[Kategori Baru di Inferensi] --> B{Apakah Ada di Kosakata Latih?}
    B -->|Tidak| C[Kelompokkan ke Kategori Khusus: 'Unseen' / 'Other']
    B -->|Ya| D[Terapkan Nilai Encoding Terlatih]
    C --> E[Gunakan Parameter Rata-rata Latih Sebagai Fallback]
```
Sistem yang stabil mengharuskan perancang fitur menetapkan langkah pengaman secara eksplisit. Terdapat beberapa strategi utama untuk mengantisipasi galat komputasi akibat kategori baru ini. Pertama, pengabaian kategori atau pemetaan ke angka nol. Pada *one-hot encoding*, kategori baru dapat diabaikan sehingga seluruh lajur fitur biner akan bernilai nol. Model linier mampu memproses observasi ini melalui parameter *intercept*, meski pendekatan ini tidak dapat membedakan antara entitas baru dan ketiadaan informasi. Kedua, pemetaan ke nilai *integer* khusus. Pada *ordinal encoding*, kategori baru dialokasikan ke parameter skalar di luar rentang pelatihan, misalnya -1. Model pohon mampu memproses nilai ini dengan optimal karena algoritma percabangannya dapat mengevaluasi dan memisahkan nilai ekstrem tersebut ke cabang tersendiri. Ketiga, substitusi nilai rata-rata global. *Target encoding* memetakan kategori baru menggunakan hitungan rata-rata target dari keseluruhan data latih ($\hat{y}_{unseen} = \frac{1}{N} \sum y_i$). Estimasi netral ini mencegah model memberikan skor prediksi asimetris pada kelas yang tidak memiliki catatan historis.

Selain pendekatan pemetaan statis, arsitek sistem dapat mendesain mekanisme pengaman melalui metode lain. Sebagai contoh, penggabungan prediktif untuk kategori berfrekuensi rendah. Sistem membuat atribut penampung dengan menggabungkan label minoritas menjadi satu kategori \"Lainnya\" selama fase pelatihan, dan secara konstan entitas baru akan dialihkan ke kategori ini pada tahap inferensi. Pendekatan alternatif adalah pemetaan berbasis kemiripan teks, yang memungkinkan sistem untuk mengevaluasi jarak metrik Levenshtein antara label baru dengan daftar referensi yang ada di memori. Metode ini berfungsi efisien untuk mengoreksi kesalahan pengetikan (*typo*), sehingga masukan anomali seperti \"Jakrta\" tetap diklasifikasikan sebagai \"Jakarta\".

Pendekatan di atas memastikan arsitektur *pipeline* berjalan lancar tanpa interupsi. Dimensi fitur yang dikirimkan ke estimator selalu konsisten antara waktu pelatihan dan waktu *inference*, sehingga stabilitas model tetap terjaga.

## Studi Kasus: Perbandingan *Encoding* pada Data Berkardinalitas Tinggi

Studi kasus prediksi harga ritel yang mencakup puluhan ribu parameter kode pos digunakan untuk menguji batasan komputasi berbagai teknik *encoding*. Fitur berkardinalitas tinggi semacam ini menuntut representasi yang mampu menjaga sinyal prediktif tanpa memicu penambahan dimensi secara drastis. Evaluasi perbandingan antarmetode ini harus dieksekusi menggunakan prosedur *nested cross-validation* untuk memastikan metrik yang diperoleh dari *target encoding* bebas dari indikasi penyerapan data masa depan (*data leakage*) pada lipatan partisi pengujian.

Hasil perbandingan metode pemetaan matriks tersebut menyoroti tiga pola konsekuensi utama. Pola pertama adalah ekspansi dimensi secara tidak seimbang (*sparse matrix*). Metode *one-hot encoding* memetakan keseluruhan variabel kode pos ke dalam kolom biner yang berdiri sendiri, sehingga seketika mengubah matriks operasional menjadi tumpukan puluhan ribu kolom tambahan. Ekspansi struktural ini langsung mengonsumsi kapasitas memori sistem, memperlambat proses kalkulasi konvergensi, serta meningkatkan probabilitas *overfitting*. Pola kedua berpusat pada kompresi deterministik. Penerapan algoritma *hashing* mempertahankan batas lebar kolom matriks statis yang efektif menekan pelebaran dimensi. Akan tetapi, metode ini memunculkan probabilitas tabrakan (*collision*) tinggi yang memungkinkan dua wilayah kode pos dengan karakteristik distribusi bertolak belakang dinormalisasi dalam indeks vektor yang identik. Pola ketiga adalah pembentukan representasi matriks padat (*dense*). Pendekatan *target encoding* maupun *entity embedding* mengubah label setiap kategori menjadi bobot numerik berdensitas tinggi tanpa memperlebar variabel matriks asli. Secara matematis, *entity embedding* mengevaluasi kategori dalam ruang vektor jaringan saraf, sedangkan *target encoding* mengompresi matriks tersebut melalui parameter probabilitas agregasi data sasarannya.

Keunggulan *target encoding* pada data berkardinalitas tinggi, terutama saat menghadapi kategori langka, dikendalikan oleh mekanisme penaksir *empirical Bayes* atau *shrinkage*. Mekanisme penyusutan ini menstabilkan representasi melalui rumusan:

$$S_i = \lambda_i \bar{y}_i + (1 - \lambda_i) \bar{y}$$

Dalam persamaan fungsi Bayesian di atas, parameter $S_i$ adalah nilai substitusi yang dikalkulasi untuk label kategori ke-$i$. Variabel $\bar{y}_i$ merupakan rata-rata sasaran nilai historis spesifik pada kategori ke-$i$, sedangkan parameter $\bar{y}$ menandakan metrik ukuran populasi dari pengumpulan set *dataset* pelatihan. Indikator persentase probabilitas ($\lambda_i \in [0, 1]$) dimodifikasi berdasarkan evaluasi frekuensi munculnya label tersebut. Manakala satu label kode pos sangat jarang dijumpai pada iterasi pengamatan data, kalkulasi untuk metrik $\lambda_i$ menurun proporsional menuju angka nol. Operasi komputasi ini memastikan substitusi representasi parameter data sampel yang tak terduga kembali mengacu pada statistik probabilitas sentral ($\bar{y}$). Evaluasi matematis ini mengantisipasi model algoritma untuk menghasilkan klasifikasi yang keliru pada distribusi variabel minoritas.
```{mermaid}
%%| label: fig-ch04-4_2
%%| fig-cap: "Matriks Transformasi Lajur Tunggal Nominal Menjadi Matriks Fitur One-Hot Encoding"
%%{init: {'theme': 'neutral', 'themeVariables': { 'edgeLabelBackground': '#ffffff' }}}%%
graph TD
    A[Kolom Kategori Mentah: Sepatu, Tas, Baju] -->|Satu Kolom| B[One-Hot Encoder]
    B --> C[Tiga Kolom Baru: Kolom_Sepatu, Kolom_Tas, Kolom_Baju]
```
Bukti pengukuran pada lingkungan sistem produksi menunjukkan bahwa representasi padat memberikan performa klasifikasi yang jauh lebih tangguh. Metode pemetaan densitas tinggi terbukti lebih stabil menahan distorsi data dibanding pendekatan matriks tersebar lebar, menjadikannya pilihan prioritas untuk mengurai tumpukan fitur berkardinalitas tinggi di lapangan.

## Sintesis Keputusan dan Prinsip Akhir

Representasi data kategorikal menuntut pemetaan yang presisi dengan menyeimbangkan efisiensi dimensi ruang fitur dan kedalaman sinyal prediktif.

**Panduan Pemilihan Encoding.** Gunakan *ordinal encoding* murni untuk data bertingkat (seperti ukuran pakaian atau peringkat layanan) saat dilatih menggunakan model berbasis pohon. Terapkan *one-hot encoding* untuk data nominal dengan kardinalitas rendah guna mengisolasi kontribusi kategori pada model linier. Untuk fitur berkardinalitas tinggi (seperti kode pos atau ID wilayah), pilih *target encoding* dengan mekanisme pemulusan (*smoothing*) Bayesian guna memampatkan dimensi tanpa kehilangan korelasi prediktif target.

**Manajemen Risiko Kebocoran.** Penggunaan *target encoding* sangat rentan terhadap kebocoran data (*data leakage*). Penegakan aturan prapemrosesan harus mengunci kalkulasi rata-rata target murni pada himpunan data latih. Di tingkat produksi, gunakan metode validasi silang internal (*cross-fitting*) atau permutasi urutan statistik (seperti CatBoost) untuk memblokir rembesan informasi target secara permanen.

**Optimasi Skala Besar.** Ketika tabel didominasi oleh jutaan kategori unik, terapkan *hashing trick* untuk membatasi ukuran matriks secara statis tanpa memelihara kamus kosakata di memori. Untuk menangkap kemiripan makna antar-kategori di dalam ruang vektor kontinu berdimensi rendah, *entity embedding* berbasis jaringan saraf adalah pendekatan modern terbaik yang menghasilkan representasi padat dan portabel bagi model hilir.

Studi komparatif mendalam mengenai unjuk kerja berbagai model pengkodean kategori ini dapat dipraktikkan langsung pada repositori kode pendamping bab ini.
