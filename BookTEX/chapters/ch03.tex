% ==========================================================================
% ch03.tex — dihasilkan dari v2/drafts oleh tools/md2tex.py
% Sumber: v2\drafts\ch03\ch03_draft_v2.md
% Boleh disunting manual; menjalankan md2tex.py lagi akan menimpa.
% ==========================================================================
\chapter{Representasi Fitur Numerik}\label{representasi-fitur-numerik}

Fitur numerik sering dianggap lebih siap masuk ke model dibandingkan teks atau gambar, tetapi angka mentah tidak otomatis menjadi representasi yang baik. Fitur yang berbeda hidup pada rentang yang berbeda. Beberapa nilai ekstrem mungkin sah dan penting, sedangkan yang lain dapat mendominasi skala seluruh fitur. Distribusi yang menjulur (skewed) dapat membuat model linear kesulitan membaca pola, sedangkan model berbasis jarak dapat terlalu mengikuti fitur yang satuannya besar.

Bab ini membahas cara membentuk ulang fitur numerik agar lebih sesuai dengan model yang digunakan. Persoalan skala dan bentuk distribusi perlu dibedakan karena keduanya menuntut penanganan yang berbeda. Standardisasi, penskalaan min-max, penskalaan tangguh (robust scaling), transformasi pangkat (power transform), transformasi kuantil, pemotongan nilai (clipping), dan pengelompokan nilai (binning) dibahas bersama risikonya masing-masing. Bab ini menguraikan pemilihan transformasi berdasarkan cara keluarga model membaca fitur, baik melalui jarak, gradien, maupun pemisahan (split).

\section{Skala dan Bentuk Distribusi}\label{skala-dan-bentuk-distribusi}

Contoh kerja pada bagian ini menggunakan dataset Online News Popularity. Setiap baris merekam satu artikel berita \emph{online} dengan metadata numerik, termasuk jumlah tautan (\passthrough{\lstinline!num_hrefs!}), statistik historis kata kunci (\passthrough{\lstinline!kw_avg_avg!}), dan jumlah pembagian artikel (\passthrough{\lstinline!shares!}). Dua fitur dapat sama-sama sah tetapi hidup pada dunia ukuran yang berbeda. Jumlah tautan biasanya berupa cacahan kecil, sedangkan statistik historis kata kunci dapat mencapai puluhan ribu. Jika fitur-fitur seperti ini dipakai bersama dalam model berbasis jarak, satu fitur dapat mendominasi perhitungan, bukan karena fitur tersebut selalu lebih penting, melainkan karena rentang angkanya lebih besar.

Inilah masalah skala. Skala menyangkut besar nilai, rentang, dan variasi yang ditempuh sebuah fitur. Fitur dengan rentang 0 sampai 1, 0 sampai 100, dan 0 sampai 10.000.000 tidak memberi pengaruh numerik yang sama pada model yang menghitung jarak, \emph{dot product}, margin, atau penalti koefisien. Dalam keadaan seperti itu, transformasi seperti \emph{standardization}, \emph{min-max scaling}, atau \emph{robust scaling} dapat membuat beberapa fitur menjadi lebih sebanding.

Skala berbeda dari bentuk distribusi. Fitur \passthrough{\lstinline!kw_avg_avg!}, misalnya, dapat berisi banyak artikel dengan nilai sedang dan sedikit artikel dengan nilai sangat tinggi. Distribusi seperti ini disebut \emph{skewed} atau berekor berat. Mengubah skalanya saja tidak otomatis membuat distribusi tersebut menjadi simetris. Nilai ekstrem masih berada di ekor, dan sebagian besar observasi masih menumpuk di daerah kecil. Untuk masalah seperti ini, kita sering membutuhkan \emph{log transform}, \emph{power transform}, \emph{quantile transform}, atau penanganan nilai ekstrem yang lebih eksplisit.

Contoh suhu dan visibilitas pada Gambar 3.1 memperlihatkan bagaimana skala mentah dapat meregangkan ruang fitur. Titik-titiknya sama, tetapi lingkungan yang tampak dekat atau jauh berubah ketika kedua sumbu dibuat lebih sebanding.

\begin{figure}[tbp]\centering
\includegraphics[width=111.6mm]{ch03-fig-1}
\caption{Skala mentah yang meregangkan ruang fitur}
\label{fig:ch03-fig-1}
\end{figure}

Transformasi numerik membentuk kembali representasi agar angka mentah lebih sesuai dengan asumsi dan mekanisme model. Jika transformasi memiliki parameter yang dipelajari, seperti rata-rata, standar deviasi, nilai minimum, maksimum, kuantil, atau batas interval, parameter tersebut harus berasal dari data \emph{train} dan dipakai ulang pada validasi, \emph{test}, serta data baru.

\section{\texorpdfstring{Standardisasi dan \emph{Min-Max Scaling}}{Standardisasi dan Min-Max Scaling}}\label{standardisasi-dan-min-max-scaling}

Setelah skala dipisahkan dari bentuk distribusi, transformasi pertama yang perlu dikuasai adalah standardisasi. Dalam standardisasi, setiap nilai dikurangi dengan rata-rata fitur, lalu dibagi dengan standar deviasi. Jika \(x\) adalah nilai mentah, maka bentuk sederhananya ditulis sebagai berikut.

\[
x' = \frac{x - \mu}{\sigma}
\]

Dengan \(\mu\) sebagai rata-rata fitur pada data \emph{train} dan \(\sigma\) sebagai standar deviasi fitur pada data \emph{train}, hasil transformasi berpusat di sekitar nol pada data \emph{train} dan mempunyai skala variasi yang lebih sebanding dengan fitur lain. Itulah sebabnya standardisasi sering menjadi pilihan awal untuk \emph{k-NN}, SVM dengan \emph{RBF kernel}, regresi linear atau logistik dengan \emph{regularization}, PCA, dan banyak metode berbasis optimisasi gradien.

Standardisasi tidak membatasi nilai ke interval tertentu. Nilai yang jauh dari rata-rata tetap dapat berubah menjadi angka besar, misalnya 4, 6, atau -5 dalam satuan standar deviasi. Ini bukan kesalahan. Standardisasi memang membuat skala variasi lebih sebanding, bukan memotong nilai ekstrem.

Pilihan lain adalah \emph{min-max scaling}. Transformasi ini memetakan fitur secara linear ke rentang tertentu, biasanya \([0, 1]\).

\[
x' = \frac{x - x_{\min}}{x_{\max} - x_{\min}}
\]

Nilai \(x_{\min}\) dan \(x_{\max}\) juga dihitung dari data \emph{train}. Jika rentang targetnya \([0, 1]\), nilai minimum \emph{train} menjadi 0 dan nilai maksimum \emph{train} menjadi 1. Transformasi ini berguna ketika komponen hilir memang mengharapkan \emph{input} dalam rentang tetap, misalnya intensitas piksel 0 sampai 255 yang dipetakan ke \([0, 1]\), atau fitur numerik yang ingin dijaga tetap berada dalam batas tertentu.

Kedua transformasi tersebut bersifat linear dan monoton untuk setiap fitur. Artinya, dalam satu fitur, urutan nilai dipertahankan. Jika 30 lebih besar dari 20 sebelum transformasi, maka hasil transformasinya juga lebih besar. Jarak relatif di dalam fitur berubah secara linear. Namun, semua jarak multivariat tidak dipertahankan. Justru tujuan \emph{scaling} adalah mengubah bobot relatif antar fitur, sehingga jarak gabungan pada beberapa dimensi memang dapat berubah.

Gambar 3.2 menunjukkan perbedaan cara membaca standardisasi dan \emph{min-max scaling}. Standardisasi memakai rata-rata dan standar deviasi sebagai jangkar, sedangkan \emph{min-max scaling} memakai minimum dan maksimum.

\begin{figure}[tbp]\centering
\includegraphics[width=111.6mm]{ch03-fig-2}
\caption{Dua cara membuat skala fitur lebih sebanding}
\label{fig:ch03-fig-2}
\end{figure}

Kelemahan utama \emph{min-max scaling} adalah ketergantungannya pada nilai ekstrem. Jika satu artikel dengan statistik kata kunci sangat tinggi menetapkan batas maksimum, sebagian besar artikel biasa dapat terdesak ke bagian kecil dekat nol. Karena itu, \emph{min-max scaling} tidak mengurangi pengaruh \emph{outlier}. Metode ini hanya memasukkan \emph{outlier} ke dalam rentang yang dipilih.

Ada juga \passthrough{\lstinline!MaxAbsScaler!}, yang membagi nilai dengan nilai absolut terbesar sehingga hasilnya berada pada rentang sekitar \([-1, 1]\) tanpa menggeser nol. Catatan ini berguna terutama untuk data jarang (\emph{sparse}) atau fitur yang memang berpusat di sekitar nol. Untuk sebagian besar pembahasan dasar, standardisasi dan \emph{min-max scaling} sudah cukup sebagai dua pola utama.

Scaler harus berada di dalam \emph{pipeline} pemodelan. Proses \emph{fit} dilakukan pada data \emph{train}, lalu parameter yang sama dipakai untuk mengubah data \emph{train}, validasi, \emph{test}, dan data inferensi. Jika scaler dihitung dari seluruh data sebelum pemisahan, evaluasi menjadi terlalu optimistis karena informasi dari data \emph{test} telah masuk ke proses representasi. Dua scaler linear ini tetap memakai rata-rata, standar deviasi, minimum, atau maksimum sebagai jangkar. Ketika nilai ekstrem menarik jangkar tersebut, masalah skala belum selesai.

\section{\texorpdfstring{\emph{Robust Scaling}}{Robust Scaling}}\label{robust-scaling}

Nilai ekstrem dapat sah, tetapi tidak selalu pantas menentukan skala semua observasi lain. Standardisasi memakai rata-rata dan standar deviasi, dua statistik yang dapat tertarik oleh \emph{outlier}. \emph{Min-max scaling} bahkan lebih langsung terpengaruh karena satu nilai minimum atau maksimum dapat menentukan seluruh rentang. Pada data popularitas berita, artikel dengan kata kunci yang pernah sangat populer bisa saja sah, walaupun nilainya terlalu jauh untuk menjadi jangkar skala semua artikel lain.

\emph{Robust scaling} menjawab masalah tersebut dengan memakai statistik yang lebih tahan terhadap \emph{outlier} (Huber 1981), yaitu median dan rentang interkuartil. Median adalah nilai tengah setelah data diurutkan. Rentang interkuartil, atau IQR, adalah selisih antara kuartil ketiga dan kuartil pertama.

\[
\text{IQR} = Q_3 - Q_1
\]

Bentuk transformasinya dapat ditulis sebagai berikut.

\[
x' = \frac{x - \operatorname{median}(X_{\text{train}})}{\operatorname{IQR}(X_{\text{train}})}
\]

Dengan cara ini, pusat fitur ditentukan oleh median, bukan rata-rata. Skala penyebutnya ditentukan oleh sebaran bagian tengah data, bukan oleh standar deviasi yang mudah membesar ketika ada nilai ekstrem. Pada \passthrough{\lstinline!RobustScaler!}, rentang kuantil bawaan adalah persentil 25 sampai 75, yaitu IQR.

Pada contoh popularitas berita, satu artikel dengan statistik kata kunci sangat tinggi dapat menentukan nilai maksimum. \emph{Min-max scaling} kemudian menekan mayoritas artikel biasa ke rentang yang sempit. \emph{Robust scaling} tidak menghilangkan nilai ekstrem tersebut, tetapi membuat sebaran kelompok utama lebih terbaca karena skala tidak ditetapkan oleh nilai paling jauh. Median dan IQR masih mewakili kelompok utama observasi dengan lebih stabil.

Gambar 3.3 membandingkan efek ini pada satu distribusi. Titik ekstrem masih ada setelah \emph{robust scaling}, tetapi kelompok utama tidak lagi terlalu terkompresi oleh batas minimum dan maksimum.

\begin{figure}[tbp]\centering
\includegraphics[width=111.6mm]{ch03-fig-3}
\caption{\emph{Min-max scaling} dan \emph{robust scaling} ketika ada nilai ekstrem}
\label{fig:ch03-fig-3}
\end{figure}

\emph{Robust scaling} tidak menghapus \emph{outlier}. Nilai ekstrem tetap ekstrem setelah transformasi, bahkan kadang terlihat semakin jelas karena pusat data lebih stabil. Keadaan ini menguntungkan jika artikel yang sangat populer membawa sinyal yang memang penting. Jika nilai ekstrem perlu ditahan agar model tidak bereaksi berlebihan, \emph{clipping} atau \emph{capping} dapat digunakan seperti yang dibahas pada Bagian 3.5. Analisis mengenai kesalahan, variasi sah, atau sinyal khusus pada nilai ekstrem dibahas lebih lengkap pada Bab 5.

Seperti scaler lain, median dan IQR dihitung dari data \emph{train}, lalu nilai yang sama dipakai untuk validasi, \emph{test}, dan data baru. Jadi, representasi yang dipakai saat evaluasi tetap mengikuti kondisi pelatihan. Setelah skala dibuat lebih tahan terhadap nilai ekstrem, masih ada kasus ketika yang bermasalah bukan pusat atau penyebutnya, melainkan bentuk distribusi itu sendiri.

\section{\texorpdfstring{\emph{Power Transform} dan \emph{Quantile Transform}}{Power Transform dan Quantile Transform}}\label{power-transform-dan-quantile-transform}

Jika scaler, termasuk \emph{robust scaling}, terutama mengatur pusat dan satuan, transformasi berikutnya menyasar bentuk distribusi. Ada fitur numerik yang sangat \emph{skewed}, berekor berat, atau variansnya membesar ketika nilai fitur makin tinggi. Dalam situasi seperti ini, mengurangi rata-rata dan membagi standar deviasi tidak cukup. Kita perlu mengubah bentuk distribusi secara monoton per fitur.

Transformasi paling mudah dipahami adalah \emph{log transform}. Untuk fitur non-negatif seperti \passthrough{\lstinline!kw_avg_avg!}, \passthrough{\lstinline!num_hrefs!}, atau \passthrough{\lstinline!shares!}, bentuk yang sering dipakai ditulis sebagai berikut.

\[
x' = \ln(1 + x)
\]

Tambahan 1 membuat nilai nol tetap dapat diproses. Intuisi log sederhana. Perbedaan dari 10 ke 100 sering terasa lebih besar secara relatif daripada perbedaan dari 10.010 ke 10.100, walaupun keduanya sama-sama berbeda 90 pada skala mentah. Log menekan bagian ekor kanan lebih kuat daripada bagian dekat nol, sehingga fitur yang sangat \emph{skewed} dapat menjadi lebih mudah digunakan.

\emph{Power transform} memperluas gagasan ini. Alih-alih memilih bentuk tetap seperti log, metode ini mencari parameter pangkat \(\lambda\) dari data \emph{train}. Salah satu keluarga klasiknya adalah \emph{Box-Cox} (Box and Cox 1964), yang berlaku untuk nilai positif.

\[
x^{(\lambda)} = \begin{cases} \dfrac{x^{\lambda} - 1}{\lambda} & \lambda \neq 0 \\[4pt] \ln(x) & \lambda = 0 \end{cases}
\]

Syaratnya \(x > 0\). Parameter \(\lambda\) diestimasi dari data \emph{train} dengan \emph{maximum likelihood}, yaitu dipilih agar data hasil transformasi paling sesuai dengan tujuan stabilisasi varians dan pengurangan \emph{skew}. Di pustaka seperti scikit-learn, hasil transformasi ini sering distandardisasi ulang secara otomatis, sedangkan parameter \passthrough{\lstinline!standardize!} mengendalikan langkah tambahan tersebut. Rumus ini juga menunjukkan bahwa log bukan trik terpisah. Log adalah kasus khusus \emph{Box-Cox} ketika \(\lambda = 0\).

Di sisi lain, \emph{quantile transform} memakai peta yang lebih kuat. Transformasi ini mengestimasi fungsi distribusi empiris dari sebuah fitur, lalu memetakan nilai ke distribusi target, misalnya uniform atau normal. Karena peta ini dipelajari dari kuantil data, fitur yang awalnya sangat \emph{skewed} dapat dipaksa memiliki bentuk marginal yang jauh lebih rapi.

Kekuatan ini datang bersama risiko. \emph{Quantile transform} bersifat nonlinear. Transformasi ini dapat mengubah jarak antar nilai dan struktur korelasi antar fitur, terutama jika hubungan linear semula penting bagi model. Nilai baru di luar rentang yang dipelajari juga dapat terdorong ke batas distribusi target. Log memberi intervensi paling ringan karena bentuknya tetap. \emph{Power transform} mempelajari satu parameter bentuk, sedangkan \emph{quantile transform} mempelajari peta distribusi yang jauh lebih \emph{data-driven}. Karena itu, keandalan peta ini bergantung pada jumlah sampel pelatihan. Pada \emph{dataset} kecil, log atau \emph{power transform} sering lebih sesuai.

Gambar 3.4 menempatkan perbedaan ini secara visual. \emph{Scaling} hanya mengubah satuan pada sumbu. Log, \emph{power transform}, dan \emph{quantile transform} mengubah bentuk sebaran nilai. Ekor kanan dapat memendek, nilai yang semula bertumpuk dapat menyebar, dan model yang peka terhadap bentuk distribusi mendapat representasi yang lebih mudah dipakai.

\begin{figure}[tbp]\centering
\includegraphics[width=111.6mm]{ch03-fig-4}
\caption{Mengubah bentuk distribusi fitur numerik}
\label{fig:ch03-fig-4}
\end{figure}

Secara implementasi konseptual, \emph{power transform} dan \emph{quantile transform} adalah transformer yang di-\emph{fit} pada data \emph{train}. \emph{Log transform} dapat bersifat stateless, misalnya melalui \passthrough{\lstinline!FunctionTransformer!} untuk \passthrough{\lstinline!log1p!}, tetapi sebaiknya tetap diletakkan di \emph{pipeline} yang sama agar perlakuan data \emph{train}, validasi, \emph{test}, dan inferensi tetap konsisten.

\begin{pendalaman}{Yeo-Johnson dan batas praktis quantile transform}
\emph{Yeo-Johnson} memperluas ide \emph{Box-Cox} agar dapat menerima nilai nol dan negatif.
\end{pendalaman}

\[
\psi(x, \lambda) = \begin{cases} \dfrac{(x + 1)^{\lambda} - 1}{\lambda} & \lambda \neq 0,\ x \geq 0 \\[4pt] \ln(x + 1) & \lambda = 0,\ x \geq 0 \\[4pt] -\dfrac{(1 - x)^{2 - \lambda} - 1}{2 - \lambda} & \lambda \neq 2,\ x < 0 \\[4pt] -\ln(1 - x) & \lambda = 2,\ x < 0 \end{cases}
\]

Dengan \(x\) sebagai nilai fitur dan \(\lambda\) sebagai parameter transformasi yang diestimasi dari data \emph{train}, sisi positif rumus ini mirip \emph{Box-Cox} dengan pergeseran \(x + 1\). Pada sisi negatif, rumusnya dicerminkan melalui \(1 - x\), sehingga seluruh bilangan real dapat diproses. Untuk \emph{quantile transform}, batas praktisnya adalah jumlah sampel. Peta kuantil empiris tidak dapat mempunyai titik jangkar lebih banyak daripada sampel \emph{train}. Jika \passthrough{\lstinline!n_quantiles!} yang diminta lebih besar daripada jumlah sampel, scikit-learn membatasi nilai efektifnya ke jumlah sampel. Pada \emph{dataset} kecil, peta ini dapat menjadi kasar dan mudah mengikuti \emph{noise} sampel. Dalam keadaan seperti itu, log atau \emph{power transform} sering lebih sesuai sebagai langkah awal.

\section{\texorpdfstring{\emph{Clipping} dan \emph{Binning}}{Clipping dan Binning}}\label{clipping-dan-binning}

Log, \emph{power transform}, dan \emph{quantile transform} mengubah bentuk distribusi, tetapi tetap mempertahankan setiap observasi sebagai nilai kontinu. Ada kasus ketika transformasi halus masih belum cukup karena masalahnya terletak pada pengaruh nilai yang terlalu jauh. \emph{Clipping} atau \emph{capping} mengganti nilai di bawah atau di atas batas tertentu dengan nilai batas tersebut. Jika \(\theta_{\min}\) adalah batas bawah dan \(\theta_{\max}\) adalah batas atas, aturannya dapat ditulis sebagai berikut.

\[
x_{\text{clip}} = \begin{cases} \theta_{\min} & x < \theta_{\min} \\ x & \theta_{\min} \leq x \leq \theta_{\max} \\ \theta_{\max} & x > \theta_{\max} \end{cases}
\]

Berbeda dari penghapusan \emph{outlier}, \emph{clipping} tetap mempertahankan observasi. Yang diubah adalah besar pengaruh numeriknya setelah melewati ambang tertentu. Misalnya, model popularitas mungkin tidak perlu membedakan dua artikel yang sama-sama berada pada rezim sangat viral secara linear jika keduanya sudah jauh di atas mayoritas artikel. Untuk fitur dengan batas domain jelas, batas tersebut juga dapat lebih bermakna daripada membiarkan satu nilai jauh menetapkan skala.

Batas \emph{clipping} harus memiliki alasan. Ada batas domain yang ditentukan sebelum melihat data, seperti rentang sensor atau batas fisik. Ada juga batas statistik yang dipelajari dari data \emph{train}, misalnya persentil, IQR, atau MAD. Batas domain tidak perlu melalui \emph{pipeline} pelatihan karena ditetapkan sebelum data dibagi. Batas statistik harus dipelajari hanya dari data \emph{train}. Tabel 3.1 merangkum beberapa aturan umum. Kolom pertama menyebut metode, kolom kedua menyebut bentuk batas, dan kolom ketiga memberi petunjuk kapan metode itu wajar dipakai.

\tabeljudul{Tabel 3.1 — Aturan penetapan batas untuk \emph{clipping} atau \emph{winsorization}}

{\def\LTcaptype{none} % do not increment counter
\par\vspace{3pt}\noindent
\begingroup\renewcommand{\arraystretch}{1.0}%
\scriptsize\linespread{1}\selectfont
\renewcommand{\_}{\textunderscore\hspace{0pt}}%
\begin{xltabular}{\linewidth}{@{}>{\raggedright\arraybackslash}p{(\linewidth-4\tabcolsep)*\real{0.160}} >{\raggedright\arraybackslash}p{(\linewidth-4\tabcolsep)*\real{0.394}} >{\raggedright\arraybackslash}p{(\linewidth-4\tabcolsep)*\real{0.446}}@{}}
\toprule

Metode
 & 
Aturan batas
 & 
Kapan cocok
 \\
\midrule
\endhead




Batas domain & Batas tetap dari pengetahuan bisnis atau fisik sebelum melihat data & Batasnya bermakna dan stabil, misalnya suhu, kelembapan, atau rentang sensor \\
Persentil & Batas di bawah dan\slash{}atau di atas persentil tertentu dari data \emph{train}, misalnya persentil 1 dan 99 & Menahan dua ekor distribusi secara sederhana tanpa asumsi bentuk distribusi \\
IQR fences & \(Q_1 - k \cdot \text{IQR}\) dan \(Q_3 + k \cdot \text{IQR}\) dari data \emph{train} & \emph{Outlier} moderat dan konvensi yang selaras dengan boxplot \\
MAD & Median \(\pm c \cdot \text{MAD}\) dari data \emph{train} & Kontaminasi \emph{outlier} berat, ketika mean\slash{}std dan bahkan IQR kurang stabil \\
\bottomrule
\end{xltabular}\endgroup\par\vspace{5pt}

}

Untuk IQR, nilai \(k\) yang sering dipakai adalah 1.5 atau 3.

\[
[\,Q_1 - k \cdot \text{IQR},\ Q_3 + k \cdot \text{IQR}\,]
\]

MAD, atau median absolute deviation, mengukur median dari jarak absolut setiap nilai terhadap median. Karena pusat dan ukuran sebarannya sama-sama berbasis median, aturan ini dapat lebih tahan ketika nilai ekstrem cukup banyak. Paket seperti Feature-engine menyediakan \passthrough{\lstinline!Winsorizer!} dengan pilihan Gaussian, IQR, MAD, dan kuantil. Dalam semua pilihan statistik ini, batas dipelajari pada data \emph{train} lalu diterapkan pada data berikutnya.

\passthrough{\lstinline!MinMaxScaler(clip=True)!} dapat menahan nilai data baru agar tetap berada dalam rentang fitur yang dikonfigurasi. Opsi ini mengatur perilaku batas pada data yang datang setelah \emph{fit}, tetapi tidak menggantikan penanganan \emph{outlier} dan tetap dapat mengubah distribusi data \emph{test}.

Intervensi lain adalah \emph{binning} atau \emph{discretization}, yaitu mengubah fitur kontinu menjadi interval. Jumlah tautan dapat dibagi menjadi sedikit, sedang, dan banyak. Panjang konten dapat dibagi menjadi pendek, sedang, dan panjang. Popularitas historis kata kunci dapat dibagi ke dalam kuantil. Interval dapat dipilih dengan lebar sama (\emph{uniform}), frekuensi sama (\emph{quantile}), kedekatan ke pusat klaster satu-dimensi (\emph{kmeans}), atau secara \emph{supervised} memakai target.

Gambar 3.5 memperlihatkan hubungan antara nilai mentah, nilai yang di-\emph{clip}, dan interval \emph{binning}. Nilai di dalam satu bin tidak lagi dibedakan, sehingga informasi halus di dalam interval hilang.

\begin{figure}[tbp]\centering
\includegraphics[width=111.6mm]{ch03-fig-5}
\caption{\emph{Clipping} dan \emph{binning} pada satu sumbu numerik}
\label{fig:ch03-fig-5}
\end{figure}

\emph{Binning} dapat membantu model linear menangkap efek ambang. Jika peluang artikel populer berubah setelah jumlah tautan atau panjang konten melewati zona tertentu, indikator bin dapat membuat model sederhana membaca pola yang tidak linear. Namun, \emph{binning} juga mengorbankan detail karena dua nilai yang berada dalam interval sama diperlakukan identik. Jika hasil \emph{binning} di-\emph{one-hot}, jumlah fitur juga bertambah, sehingga isu seleksi fitur pada Bab 7 menjadi relevan.

Untuk model pohon, manfaat \emph{binning} eksternal biasanya lebih kecil karena pohon dapat belajar ambang pada fitur kontinu. Yang perlu lebih diwaspadai adalah \emph{supervised discretization}. Pada pendekatan ini, misalnya dengan \passthrough{\lstinline!DecisionTreeDiscretiser!}, sebuah pohon dangkal dilatih untuk tiap fitur dengan target, lalu daun atau batas daunnya dipakai sebagai bin. Karena target ikut menentukan batas, metode ini sangat mudah memberi kesan performa tinggi jika dilakukan sebelum pemisahan data. Praktik pipeline yang benar wajib diterapkan dengan menempatkan pemilihan batas di dalam lipatan \emph{train}, bukan pada seluruh \emph{dataset}.

\begin{pendalaman}{Spline, alternatif mulus untuk binning}
\emph{Binning} keras membuat representasi berbentuk tangga. Setiap batas bin menimbulkan lompatan, sementara variasi di dalam bin tidak terlihat. \passthrough{\lstinline!SplineTransformer!} menawarkan jalan lain dengan membentuk fitur basis B-spline, yaitu polinomial potongan yang tersambung mulus pada titik \emph{knot}. Untuk model linear yang membutuhkan non-linearitas, \emph{spline} sering lebih halus daripada bin \emph{one-hot} karena tidak ada tebing tajam di batas interval. Penempatan \emph{knot} dapat mengikuti pola yang mirip dengan batas bin, misalnya \emph{uniform} atau \emph{quantile}. Meski demikian, \emph{binning} tetap menarik ketika interpretabilitas interval lebih penting atau ketika domain memang memiliki ambang diskret yang nyata.
\end{pendalaman}

\section{Memilih Transformasi Berdasarkan Keluarga Model}\label{memilih-transformasi-berdasarkan-keluarga-model}

Daftar transformasi tadi belum menentukan pilihan akhir, karena gangguan yang sama dapat penting atau hampir tidak terlihat bergantung pada mekanisme model dalam mengolah fitur. Model yang memakai jarak, \emph{dot product}, margin, gradien, atau penalti koefisien cenderung sensitif terhadap skala. Model yang membuat keputusan melalui ambang pada satu fitur, seperti pohon keputusan, cenderung lebih tahan terhadap \emph{scaling} linear.

Tabel 3.2 memberi peta awal. Kolom ``kebutuhan \emph{scaling}'' menunjukkan seberapa sering transformasi skala menjadi langkah penting sebelum pemodelan, sedangkan kolom alasan menjelaskan mekanisme teknisnya. Tabel ini memberi panduan keputusan, bukan aturan mutlak.

\tabeljudul{Tabel 3.2 — Peta kepekaan keluarga model terhadap transformasi numerik}

{\def\LTcaptype{none} % do not increment counter
\par\vspace{3pt}\noindent
\begingroup\renewcommand{\arraystretch}{1.0}%
\scriptsize\linespread{1}\selectfont
\renewcommand{\_}{\textunderscore\hspace{0pt}}%
\begin{xltabular}{\linewidth}{@{}>{\raggedright\arraybackslash}p{(\linewidth-4\tabcolsep)*\real{0.249}} >{\raggedright\arraybackslash}p{(\linewidth-4\tabcolsep)*\real{0.231}} >{\raggedright\arraybackslash}p{(\linewidth-4\tabcolsep)*\real{0.520}}@{}}
\toprule

Keluarga model
 & 
Kebutuhan \emph{scaling}
 & 
Alasan singkat
 \\
\midrule
\endhead




k-NN \slash{} k-means & Tinggi & Jarak berubah ketika satu fitur memiliki satuan jauh lebih besar \\
SVM RBF & Tinggi & Perilaku jarak pada \emph{kernel} bergantung pada skala \\
Linear\slash{}logistik + \emph{regularization} & Sedang-tinggi & Penalti koefisien mengasumsikan skala fitur relatif sebanding \\
PCA pada fitur numerik dengan satuan atau skala tak sebanding & Tinggi jika bobot relatif antarfitur ingin disetarakan & Varians mentah yang besar dapat mendominasi komponen. \emph{Scaling} mengubah tujuan PCA \\
\passthrough{\lstinline!TruncatedSVD!} pada matriks \emph{sparse} & Bergantung pada makna bobot fitur, umumnya tanpa \emph{centering} & Tidak melakukan \emph{centering} menjaga \emph{sparsity}, sedangkan \emph{scaling} hanya dipakai jika pembobotan relatifnya memang diinginkan \\
Neural network \slash{} SGD & Sering tinggi & Optimisasi lebih mudah ketika skala \emph{input} sebanding \\
Tree \slash{} random forest \slash{} GBDT & Rendah untuk \emph{scaling} linear & Split ambang bergantung terutama pada urutan, bukan besar satuan \\
\bottomrule
\end{xltabular}\endgroup\par\vspace{5pt}

}

Pada \emph{k-NN}, dua observasi dianggap dekat atau jauh berdasarkan jarak. Jika satu fitur memakai statistik historis kata kunci yang bisa mencapai puluhan ribu dan fitur lain memakai skor sentimen yang berada di sekitar -1 sampai 1, fitur berentang besar dapat menguasai jarak total. Pada \emph{k-means}, pusat klaster juga ditarik oleh skala fitur. Pada SVM dengan \emph{RBF kernel}, skala mengubah perilaku jarak dalam \emph{kernel}. Pada PCA untuk fitur numerik dengan satuan yang tidak sebanding, varians mentah dapat membuat satu fitur mendominasi hanya karena satuannya. Standardisasi tepat ketika bobot relatif yang sebanding memang menjadi tujuan, tetapi juga mengubah objektif PCA. Pengecualian pentingnya adalah \passthrough{\lstinline!TruncatedSVD!} pada matriks \emph{sparse}, yang lazim dipakai tanpa \emph{centering} agar \emph{sparsity} terjaga. Kebutuhan \emph{scaling}-nya bergantung pada makna pembobotan fitur, bukan aturan tinggi yang universal.

Contoh Online News Popularity pada Gambar 3.6 membuat hal ini lebih konkret. Dua fitur, \passthrough{\lstinline!num_hrefs!} dan \passthrough{\lstinline!kw_avg_avg!}, memiliki rentang dan satuan yang berbeda. Pada ruang mentah, batas keputusan \emph{k-NN} mudah mengikuti geometri yang dibentuk oleh skala awal. Setelah standardisasi, kedua sumbu menjadi lebih sebanding, sehingga bentuk batas keputusan dapat berubah.

\begin{figure}[tbp]\centering
\includegraphics[width=111.6mm]{ch03-fig-6}
\caption{Batas keputusan \emph{k-NN} sebelum dan sesudah standardisasi}
\label{fig:ch03-fig-6}
\end{figure}

Model pohon berbeda. Pohon dapat membuat aturan seperti ``suhu \textless= ambang'' atau ``energi lampu \textless= ambang''. Jika satuan fitur diubah secara linear, urutan observasi tidak berubah. Ambangnya berubah angka, tetapi pemisahan yang sama masih tersedia. Inilah intuisi mengapa decision tree, random forest, dan banyak GBDT relatif rendah sensitivitasnya terhadap \emph{scaling} linear.

Kepekaan yang rendah tidak berarti pengaruh transformasi hilang sama sekali. Transformasi yang mengubah urutan, menggabungkan nilai, atau membuang informasi tetap dapat mengubah perilaku pohon. \emph{Binning} yang terlalu kasar, misalnya, dapat menghilangkan ambang yang sebenarnya bisa dipelajari pohon dari nilai kontinu. Detail implementasi seperti penanganan nilai hilang, presisi numerik, dan \emph{histogram-based binning} juga dapat membuat representasi tetap berpengaruh.

Transformasi numerik sebaiknya dipilih sebagai bagian dari \emph{pipeline} dan dibandingkan secara empiris. Standardisasi adalah \emph{baseline} yang kuat untuk banyak model sensitif skala. \emph{Robust scaling} sesuai ketika nilai ekstrem sah tetapi tidak boleh menentukan skala. Transformasi log, \emph{power}, atau \emph{quantile} dipilih ketika bentuk distribusi menjadi masalah. \emph{Clipping} dan \emph{binning} dipakai ketika ada alasan domain atau alasan pemodelan yang jelas.

\begin{pendalaman}{Binning internal pada gradient boosting modern}
Implementasi GBDT modern seperti LightGBM memakai algoritma berbasis histogram. Nilai kontinu dibucketkan ke sejumlah bin diskret sebelum pencarian split. Model kemudian menilai batas bin, bukan setiap nilai mentah satu per satu. Bagi praktisi, ada dua akibat. Pertama, \emph{scaling} linear eksternal biasanya redundan untuk model seperti ini. Kedua, \emph{pre-binning} yang terlalu kasar dari luar dapat berinteraksi buruk dengan binning internal model, bahkan membuang informasi yang seharusnya masih dapat dimanfaatkan. Karena itu, tabel utama menyebut sensitivitasnya ``rendah'', bukan ``nol''.
\end{pendalaman}

\begin{sintesis}

Benang merah bab ini adalah diagnosis, bukan mencari satu scaler baku untuk semua angka. Jika fitur hanya berbeda satuan atau rentang, standardisasi atau \emph{min-max scaling} mungkin cukup. Jika nilai ekstrem sah tetapi tidak boleh mengatur skala seluruh fitur, \emph{robust scaling} lebih sesuai. Jika bentuk distribusi yang menjadi masalah, terutama \emph{skew} atau ekor berat, log, \emph{power transform}, atau \emph{quantile transform} dapat dipertimbangkan. Jika pengaruh nilai ekstrem perlu dibatasi atau pola ambang perlu diekspresikan, \emph{clipping} dan \emph{binning} menjadi pilihan yang lebih langsung.

Kompromi utamanya adalah antara kesederhanaan, informasi yang dipertahankan, dan kecocokan dengan model. Transformasi linear menjaga urutan dalam fitur tetapi mengubah geometri antar fitur. Transformasi bentuk distribusi dapat membantu, tetapi juga dapat mengubah jarak dan korelasi. \emph{Clipping} menahan pengaruh ekstrem, tetapi dapat menyembunyikan variasi yang masih berguna. \emph{Binning} membuat ambang lebih mudah dibaca, tetapi menghapus detail di dalam interval.

Keputusan akhir perlu dimulai dari diagnosis atas representasi mentah dan keluarga model yang akan memakai fitur tersebut. Parameter transformasi harus dipelajari hanya dari data \emph{train}, lalu hasilnya diperiksa melalui evaluasi yang sah. Dengan cara ini, fitur numerik menjadi representasi yang sengaja dirancang untuk tujuan pemodelan.

\end{sintesis}

\begin{bacaan}

\begin{itemize}
\tightlist
\item
  scikit-learn --- Preprocessing data --- \url{https://scikit-learn.org/stable/modules/preprocessing.html}. Penskalaan, transformasi, dan diskretisasi fitur numerik.
\item
  scikit-learn --- Importance of Feature Scaling --- \url{https://scikit-learn.org/stable/auto_examples/preprocessing/plot_scaling_importance.html}. Contoh dampak penskalaan pada model berbasis jarak.
\item
  Feature-engine --- Winsorizer --- \url{https://feature-engine.trainindata.com/en/latest/api_doc/outliers/Winsorizer.html}. Pemotongan (clipping) nilai ekstrem sebagai transformer.
\item
  LightGBM --- Features (histogram binning) --- \url{https://lightgbm.readthedocs.io/en/latest/Features.html}. Binning berbasis histogram di dalam model pohon.
\item
  Box \& Cox (1964), JRSS-B --- \url{https://doi.org/10.1111/j.2517-6161.1964.tb00553.x}. Asal transformasi Box--Cox.
\item
  Yeo \& Johnson (2000), Biometrika --- \url{https://doi.org/10.1093/biomet/87.4.954}. Transformasi daya untuk nilai nol/negatif.
\end{itemize}

\end{bacaan}

\rujukanheading

\protect\phantomsection\label{refs}
\begin{CSLReferences}{1}{1}
\bibitem[\citeproctext]{ref-boxcox1964}
Box, George E. P., and David R. Cox. 1964. {``An Analysis of Transformations.''} \emph{Journal of the Royal Statistical Society, Series B} 26 (2): 211--52.

\bibitem[\citeproctext]{ref-huber1981robust}
Huber, Peter J. 1981. \emph{Robust Statistics}. Wiley.

\end{CSLReferences}
